# Pinned to the versions in the project venv (cx/). Upgrade deliberately:
# bump a pin, run the test suite, then deploy.
Django==5.2.10
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
psycopg2-binary==2.9.12
celery[redis]==5.6.3
Pillow==12.2.0
openpyxl==3.1.5
reportlab==4.4.10
# Invoice/receipt PDF rendering (HTML→PDF). Needs native libs on the host
# (libpango, libcairo, libgdk-pixbuf, libffi) — install them in the deploy build
# (e.g. build.sh / Dockerfile). vs_finance.documents imports it lazily and degrades
# to a 503 on the .pdf endpoints if it (or its native libs) is unavailable.
weasyprint==62.3
gunicorn==25.1.0
whitenoise==6.12.0
drf-spectacular==0.29.0
python-decouple==3.8
user-agents==2.2.0
shortuuid==1.0.13
