## Undone





- School-facing procurement, in August scope, going through the FAL (2026-07-28, four user decisions): office staff raising requisition→approval→PO→receiving→vendor payment. DECIDED: (a) the FAL exposes FULL PORTS for every procurement action (raise/approve/order/receive/pay), matching how the fees side is specified - keep them thin pass-throughs to the existing vs_procurement services, NOT a re-implementation, or the surface doubles for no safety gain; (b) a newly onboarded school is seeded with approval RULES but NO approver assigned, so procurement stays blocked until the school assigns one (secure by default, and the blocked state explains itself); (c) the submit-without-approval override permission is granted to NOBODY by default - a school hands it to a named person deliberately; (d) the procurement component is specified and built ALONGSIDE the core bridge (school→books), since it only needs the school to exist, not students or terms. REMAINING: add the procurement component to the FAL spec (v1.1 has ProcurementReaderPort reads only); grant procurement permission keys to school roles; seed approval templates at onboarding per (b). vs_procurement resolves scope via vs_finance.views.resolve_entity (views/requisitions.py:116) so it shares the request.school middleware precondition - verify for school users. Design: ~/Downloads/August_Sprint/Procurement_Design_Prompt.md. NOTE: the two FAL .docx exports go stale again once the procurement component lands - regenerate from the canonical markdown before circulating.
- DEPLOY NOTE for Round 3 (2026-08-12): migration 0025 MUST run in the same deploy as the code — seeded stages and already-published stages must not disagree about how approvers are routed.
- August sprint: implement the FAL under `apps/schools/` (e.g. apps/schools/core/fal/) — NOT core/fal/, corrected 2026-08-12 by the user: the FAL is school-centric by design (it exists because school concepts meet the finance engines), so school vocabulary belongs in it; core stays domain-neutral and the FAL is the boundary where school words stop. The apps/schools/ restructure it depended on LANDED 2026-08-16 (see Done), so the package it goes in exists. Originally scoped 2026-07-04 as: copy the hardened spec stubs from ~/Downloads/August_Sprint/FAL/XVS_Finance_Abstraction_Layer_Spec_v1/core/fal/ (canonical spec: docs/fal/FAL_Interface_Spec.md v1.1 there) and wire the django_finance adapter. FAL wave 1 (school→entity + student→customer resolvers) is scheduled Aug 1–7. All 5 spec-§16 product questions DECIDED 2026-07-04 (see spec §16 Decisions): PaymentPort read-only in v1.1 (apply_payment/family allocation → v1.2 Sept), FAL-owned fee↔term link table, one primary entity per school + entity_ref override, family payments within-school only, parent portal authz = guardian↔student ownership checks (no parent RBAC keys). Unblocked.
- Move the three school dev-seed commands into `apps/schools/` (2026-08-16, surfaced by the restructure, NOT done - it needs a decision, not just a move). The engine-boundary grep in docs/architecture/school-decoupling-scope.md §11.1 now returns exactly three lines, all management commands: `core/management/commands/seed_package.py` (100% school - PackagePlan/BillingCycle, an unambiguous move), `core/management/commands/seed_dev_data.py:312` (seeds the WHOLE platform and only touches schools in one block, so moving it wholesale would put a tenant/rbac/finance seeder inside the school package - the right shape is probably to split the school block out), and `vs_notifications/management/commands/seed_notification_settings.py:59` (a notifications command that iterates schools - arguably belongs where it is, and its import is already late/deliberate). The scope doc predicted these and prescribed "move the school dev seeds into the school package when apps/schools/ is created"; that is now, but the three are not the same kind of thing. Until they move, the engine boundary is clean in application code and dirty only in dev tooling.
- August sprint FAL ripple guards (2026-07-05, from spec §16 decisions): (a) M9 provisioning must be the ONLY creator of TENANT LedgerEntities for a school - today finance.entity.create can still mint a second entity for the same school, which would trip the new AmbiguousPrimaryEntity loud-error; add a guard (block/warn on second TENANT entity per school) + alerting for AmbiguousPrimaryEntity when FAL wave 1 lands. (b) M28 guardian-ownership authz has a hard dependency on the guardian↔student link model in vs_students (M11) - already sequenced M11-before-M28, but the M28 FRD must state it. (c) August parent-payment bridge exposes per-invoice payment sessions only (initiate_collection takes one invoice); multi-child single-session payment waits for FAL v1.2. (d) Portal UX: standalone (no-invoice) parent payments park as customer credit - always pass an invoice from the portal, or surface the credit balance.






## Done

# STEP 0 IS DONE: `apps/schools/` EXISTS AND vs_schools LIVES IN IT (2026-08-16). `git mv apps/vs_schools apps/schools/vs_schools`, plus a package docstring in apps/schools/__init__.py stating the one-way dependency rule. THE LABEL DID NOT MOVE and that is the whole point: `VsSchoolsConfig` now sets `name = "schools.vs_schools"` and pins `label = "vs_schools"` explicitly, so all 7 tables are still `vs_schools_*`, the nine other apps' historical migrations still resolve their `("vs_schools", "0001_initial")` dependencies, and `makemigrations --check` says "No changes detected" before and after. Only the PYTHON IMPORT PATH moved: 40 import sites across 24 files became `from schools.vs_schools...`, plus 4 consumer strings in vs_config/runtime_settings.py and 2 `choice_labels()` dotted paths in the school app's export datasets (those two RESOLVE BY IMPORT, so unlike the migration strings they had to change - verified they still resolve to the real classes).
# THE MOVE SILENTLY BLINDED THE ENGINE-BOUNDARY CHECK, and that was the one real defect in it. docs/architecture/school-decoupling-scope.md §11.1 is the grep CLAUDE.md leans on to prove no engine imports the school app, and it matched the literal `from vs_schools`. After the move an engine importing the school app writes `from schools.vs_schools.models import ...`, which that pattern does NOT match - so the check would have kept reporting "clean" while the boundary it guards stood open. Both recipes in §11.1 now match `from schools.` / `import schools.` as well, and the exclusion is `^apps/schools/`. Fixed in the same change as the move, because a guard that goes quiet is worse than no guard.
# VERIFIED, one app at a time on DB_NAME=cx_step0_move, never --parallel: 2,147 tests OK across all 14 apps whose imports were touched - schools.vs_schools 62, vs_tenants 28, vs_exports 152, vs_config 61, vs_admin_console 130, core 46, vs_rbac 280, vs_user 105, vs_import_data 7, vs_tickets 35, vs_notifications 85, vs_payments 139, vs_finance 532, vs_procurement 485. The count that mattered most was 62: moving a package is exactly how a test suite silently half-discovers, so the three moved test files were counted by hand (12+26+24=62) and matched what ran. NOTE FOR NEXT TIME: the test label is now `schools.vs_schools`, not `vs_schools` - Django resolves a test label as a module path, not an app label, so the old command finds nothing.
# CONTAMINATION SEEN AND RULED OUT, worth knowing because it looked like a real failure: vs_config first exited 1 with NO "Ran N tests" line, on `ImportError: cannot import name 'ABANDONED_QUEUED_HOURS' from 'vs_exports.constants'` - while a CONCURRENT SESSION was editing apps/vs_exports/{constants,services,audit,tasks,tests}.py and apps/apps/celery.py in this same working tree. Nothing in step 0 touches vs_exports. Re-run after their edits settled: 61 tests OK. Two suites were also running at once for a while, which is the exact contention CLAUDE.md warns about; swap peaked at 6.8G of 8G.
# DOCUMENTATION GATE: latest MRD (v2.16) and latest M01 FRD (v1.2) were both opened and searched. Each names `vs_schools` ONLY as an app label under code ownership, never as a filesystem path, and the label is unchanged - so no capability, status, contract, permission or limitation moved. Pure refactor, no version bump required.
# NOT DONE, deliberately, and now the top item in Undone: the three school dev-seed commands that still sit outside apps/schools/.

# THE THREE FRONTEND ITEMS ARE CLOSED (2026-08-16), all in console-fe, and TWO OF THE THREE WERE ALREADY DONE - audited in the code first rather than believed, which is now the third time this session a note outlived the work it described. (a) THE INVOICE DRAWER was already complete: Total / Paid (cash) / Credited / Settled / Balance due render as five separate figures read from the summary, so it never derived 'paid' from the balance drop (the specific mistake the note warned about); settlements, grouped gl_journals and the full activity timeline are all there and no old `payments`/`gl_postings` key is referenced anywhere. Nothing to do. (b) THE NOTIFICATION OVERHAUL was done apart from one filter: settings already use the effective-matrix shape, updates upsert by {event_type_key, channel, is_enabled} with no row ids, and transactional rows render locked. (c) GR/IR WAS THE REAL ONE, and it was a live misreport rather than the cosmetic gap the note described - see below.
# GR/IR SHOWED A BRANCH READER A CLEAN BILL OF HEALTH NOBODY GAVE (console-fe 87afe81). The backend withholds `control_balance` and `difference` from a branch-bound caller on purpose (both come from the GL, which has no branch column). But `kobo()` is `m?.kobo ?? 0`, so the withheld null became 0, and the Difference card renders `diff === 0 ? 'Reconciled' : 'Variance to investigate'` in GREEN. A bursar narrowed to one branch was told GR/IR reconciled when nobody had computed it. THE TYPE WAS COMPLICIT: `control_balance` was declared non-nullable, so the compiler asserted a contract the API had stopped honouring - which is why the fix started at the type and let tsc find the rest. Now reads 'Not shown' in gray with the reason, plus `unassigned_excluded_count` surfaced on the four screens that have a consumer (as a COUNT, never money - an amount would disclose head-office spend). Negative control: 4 of 7 new screen tests fail against the pre-fix screen, including 'never badges a withheld difference as reconciled'.
# ALSO FIXED IN THE SAME PASS: history can now narrow to platform rows (console-fe 3353afd; the backend had supported `?scope=platform` all along and the console could not reach it). THE TRAP, worth knowing before touching that panel: the history endpoint REFUSES an unfiltered request, which is why the panel applies an implicit last-7-days window; scope counts as a filter server-side, so adding it could silently have lifted the window and swapped a week for all-time on a click that reads as narrowing. The window was deliberately kept. The control is deliberately TWO-STATE: the backend's has-a-filter guard accepts any non-empty scope but only narrows on the literal 'platform', so an unrecognised value would pass the guard and filter nothing. And `NotificationSetting.source` was typed 'school' when the API emits 'tenant' (console-fe e93f9d9) - nothing branched on it, so tsc never caught it.
# THE `?school=<id>` PARAMETER IN THE NOTIFICATION DOCS NEVER EXISTED ANY MORE (backend 5ba6edb, docstrings only, AST-verified identical after stripping docstrings). Scope has come from the asserted tenant since the tenant refactor; nothing reads a school or tenant QUERY param anywhere in vs_notifications. Five more false claims in the same file went with it, including the history view claiming CX staff 'see everything' when `get_queryset` hard-scopes every caller to the asserted tenant. THE ANTI-ENUMERATION GUARANTEE the old text described was CHECKED rather than deleted: it still holds one layer up, since vs_rbac.authentication returns the same NotFound for an unknown slug and for a real-but-foreign one unless a view sets `platform_cross_tenant_param` (three views do; none in vs_notifications).
# STILL OPEN in console-fe, found and deliberately not fixed: a school-tenant admin can select 'Platform only' on the history screen and always get zero rows, because the base queryset pins the caller's tenant and a school tenant is never PLATFORM-kind. Hiding the control needs the tenant KIND on the client and `src/utils/tenant-context.ts` exposes only the slug. ORIGINAL ENTRIES: FRONTEND, procurement reports (2026-08-12, from commit 69a91c7): two response-shape changes to handle, both only for branch-bound callers. (1) grir_aging's `control_balance` and `difference` are now `null` for a narrowed caller — render the withheld state, do not crash or show zero. (2) reports gain `unassigned_excluded_count`, present ONLY when the caller is narrowed — surface it so a bursar understands their figure excludes entity-level documents (mostly pre-branch historical rows) rather than silently under-reporting. Unbound callers and tenants without branches get byte-identical responses to before. || Frontend: consume the enriched invoice-detail payload (2026-07-06). GET /finance/invoices/<id>/ now returns summary.paid/credited/settled/balance, a `settlements[]` list (type = PAYMENT|CREDIT_NOTE|CONCESSION|WRITE_OFF, with reference/date/amount/method), `gl_journals[]` grouped per source document (document_type/reference/date/lines[]), and credit-note/concession/write-off entries in `activity[]`. (1) Invoice drawer summary must show Paid / Credited / Settled / Balance as separate figures - never derive "paid" from the balance drop. (2) Replace the cash-only payments table with the `settlements` list + type badges. (3) GL postings panel should render each `gl_journals` group, not just the original invoice posting. (4) Activity timeline should render all `activity` entries. Old `payments`/`gl_postings` keys are still present (backward-compat) but are cash/invoice-only - migrate off them. Backend + tests already shipped. || Frontend: adapt to the notification-system overhaul (2026-07-07, commit feb08dc). Breaking /v1/notify/ changes: (1) settings GET now returns the full EFFECTIVE matrix rows {event_type_key, event_type_label, source_module, channel, is_enabled, is_transactional, source: school|platform|default} instead of per-school setting rows with ids; (2) settings PATCH update/ upserts by {event_type_key, channel, is_enabled} - row ids are gone; transactional events are read-only (render them disabled); (3) feed GET /notifications/{id}/ returns 404 (not 403) for anything that isn't the caller's own in-app record; (4) history gains scope=platform filter; school_id still works; (5) CX staff manage platform-scope settings by default and any school via ?school=<id>. Also success/error envelopes on these endpoints now actually follow core.response ({success,message,data}/{success,message,error,code}) - the old code raised TypeError on several paths.

# APPROVAL AUTHORITY CAN NO LONGER CROSS A TENANT BOUNDARY (2026-08-16, commits d6b0a26 and 1f9e14c). TWO DOORS INTO THE SAME ROOM, both now shut, and the second was only found because the first was being fixed. (1) ORGANOGRAM stages returned platform-global seat holders straight through, the only approver source not contained; SPECIFIC_POSITION was the sharpest case because it ignores the requester entirely, so it did not need an odd chart shape to leak. (2) A DELEGATION could hand authority to a user in another tenant: the delegation queryset filters `tenant=instance.tenant`, which READS LIKE containment without being it - that column scopes the ROW and says nothing about the user the row names. Fix (1) did not cover (2), because delegation expansion runs after the choke point.
# FIXED AT THE CHOKE POINT, not on the branch that was reported: `_tenant_members` now runs once on base_users after the source dispatch, and again on the delegates, and its docstring is now the single definition of 'may hold approval authority here'. The root cause was never the organogram branch - it was that containment was a rule each source had to remember, and a source forgot. A sixth source now inherits it. THE ORDERING IS LOAD-BEARING: delegate containment runs BEFORE `excluded_delegators`, so a refused exclusive delegation cannot strip its delegator while contributing nobody; contained afterwards it would EMPTY the stage, which is worse than the bug being fixed.
# WRITE BOUNDARY TOO, for delegations: the serializer resolved `delegate` over an unscoped user queryset with no validation. It now uses vs_rbac's `TenantScopedRelatedField`, reused rather than reimplemented SPECIFICALLY so a foreign id and an absent id return one shared message - a hand-written 'that user is in another tenant' check would have closed the hole and opened the cross-tenant id oracle this project already fixed once (see the 2026-08-14 entry on vs_rbac/serializers/tenant.py). Existing bad rows are neutralised at resolution rather than deleted by a data migration: ignoring a row is reversible, destroying delegation history is not.
# DECIDED by the user when asked: a delegate must be in the SAME TENANT as the delegator, with NO platform-staff exception. Do not add one without a new decision.
# NEGATIVE CONTROLS RUN ON ALL THREE HALVES SEPARATELY, which is the evidence these tests bind to behaviour rather than to fixture values: neutralising organogram containment fails exactly 3 tests by name and leaves the 2 counterweights passing; neutralising delegate containment fails exactly 3 resolver tests and leaves the API tests passing; neutralising the write-boundary guard fails exactly the 2 API tests. Suites: vs_workflow 253 (from 240), vs_procurement 485, vs_payments 139, vs_finance 519, all OK.
# WATCH ON DEPLOY: a tenant relying on an ORGANOGRAM stage staffed from OUTSIDE itself loses those approvers, and since the seeded stages never auto-skip those documents PARK rather than route. Parking is recoverable - `repair_workflows` releases them once a same-tenant holder is seated. The delegation half cannot newly empty a stage. ORIGINAL FINDING: ORGANOGRAM APPROVERS ARE NOT TENANT-CONTAINED (found 2026-08-16 while writing the ORGANOGRAM tests, NOT fixed, no test asserts the current behaviour). In `vs_workflow/services/approvers.py`, every other approver source is contained inside the requesting tenant: WORKFLOW_GROUP goes through `_tenant_members`, and the ROLE / DYNAMIC_ROLE / override paths filter `tenant=tenant` via `_users_for_role_key`. The ORGANOGRAM branch is the ONLY one that returns `OrganogramService` results straight through with no containment, so a tenant whose staff sit under a shared platform seat can have approval authority resolved to a user in ANOTHER TENANT. The reason it is wrong is written in the codebase already: `_tenant_members`' own docstring says organogram positions are platform-global seats, so a tenant's group must never route approval authority to somebody outside that tenant - which applies at least as strongly to a stage that climbs the organogram directly. Active-user filtering IS covered (`current_holders` filters `user__is_active=True`); only tenant containment is missing. Severity is latent rather than live TODAY because central stages are ROLE-sourced and no tenant is known to publish an ORGANOGRAM stage, but nothing prevents one. Fix is to wrap the ORGANOGRAM branch in `_tenant_members` like the group path; needs a decision first, because it changes who can approve wherever such a stage exists.

# CONFIRMED CLOSED 2026-08-16 during the pre-sprint cleanup, checked in the tree rather than taken from the note above: FRD TEMPLATE DEFECTS, both found 2026-08-15 by rendering the pages (neither is visible to any structural/XML check). (1) FIXED going forward: every FRD's cover logo was the LETTER "X" typed in 36pt Aptos Display, colour 355B9D, as paragraph 0 of the body - not an image. The MRD has no such paragraph; its logo is the real brush-stroke mark (`word/media/image1.png`, 373x311 RGBA) placed in the first-page header, which is why only the MRD ever looked right. Because each new FRD is copied from an existing FRD, the placeholder propagated to every module document. `docxlib.replace_logo_placeholder` now swaps it for the real image (extracted from the MRD) and RAISES if paragraph 0 is not the placeholder, so a future template change fails loudly. Applied to M19 v1.2, M23 v1.3 and M31 v1.0; the older versions are deliberately untouched (never rewrite a previous version), so they still show the X. (2) ROOT-CAUSED AND FIXED 2026-08-15 - **the FRD template declares the wrong Dublin Core namespace**. `docProps/core.xml` uses `xmlns:dc="http://purl.org/dc/elements/1.0/"`; OPC requires `1.1`. Word cannot resolve dc:title/dc:creator/dc:subject in the unknown namespace, declares the whole package unreadable, and offers to repair - and the repaired copy is an UNNAMED "Document1", which is exactly why scripted export and every agent round-trip failed on that family while the MRD (correct namespace) always opened. It is a ONE-CHARACTER fault. Found by bisection: with an identical minimal body the MRD package rendered and the FRD package did not, then swapping each shared part from the MRD isolated core.xml. `docxlib.repair_core_properties()` now rewrites it after every save; all four new documents verified clean. **LOCAL TEST HARNESS**: `qlmanage -t` is a reliable proxy - it renders a good .docx in ~1s and HANGS on a broken one, so this class of defect can be bisected without opening Word (scratchpad `probe.sh`). RULED OUT along the way, do not re-check: zip entry order, content types, dangling rels, dangling image refs, illegal XML chars, XML well-formedness, empty cells/rows, missing tblGrid, gridSpan overflow, orphan vMerge, nested tables, style refs, numbering refs, embedded fonts, attachedTemplate, documentProtection, OLE objects, macOS quarantine, image integrity, and XSD validity (every document fails on benign w14/mc extension attributes - the MRD worst at 2,916 - so schema validity is NOT the signal). CLOSED 2026-08-15: on the user's explicit instruction (which overrides the never-rewrite-a-previous-version rule for this one repair), all 17 broken historical versions were fixed in place, and the typed-X logo was replaced with the real mark across 22 FRDs. Paragraph and table counts were compared before and after on every file - all 42 preserved - and all 42 now render. Originals backed up to the session scratchpad `frd-originals/`. One file is deliberately untouched: `01_School_and_Branch_Management_FRD_v1.0.docx` is a legacy-format document with a different visual system ("CODEX VISION" cover, no logo anywhere), not the current template.
  VERIFICATION 2026-08-16: all 44 documents in docs/frd carry the correct `dc/elements/1.1` namespace, none has the typed-X placeholder (paragraph 0 of every FRD body is a real `<w:drawing>` and the brush-stroke mark sits in the header), and ALL 44 RENDER - checked by rendering every one of them, not by XML alone, and two covers were read as images to confirm the logo. ONE CORRECTION TO THE HARNESS NOTE ABOVE: `timeout` does NOT exist on this machine, so `timeout N qlmanage ...` reports failure for EVERY file including known-good ones. Use `perl -e 'alarm N; exec @ARGV' qlmanage -t -s 500 -o <dir> <file>` instead. The small square that renders under "Internal Documentation" on the covers is QuickLook drawing an empty-paragraph marker, not content, and is not a defect.

# THE PAYOUT GATE IS ON FROM ONBOARDING, so the entry below is closed as a code gap (2026-08-16). It was closed by commit 681456f on 2026-08-14 and only the record was stale. `provision_payout_approval` (vs_payments/provisioning.py) is registered against finance's entity provisioning from `VsPaymentsConfig.ready()`, so the ladder is published INSIDE THE TRANSACTION THAT CREATES A TENANT'S BOOKS rather than by a command somebody had to remember. Seeded BLOCKED, not open: one always-on stage, `skip_if_no_approvers=False`, ROLE-sourced on `payout-approver`, and `ensure_approver_role` creates that role with NOBODY in it, so the first batch parks instead of paying itself out. Non-destructive on re-seed. Same shape as vs_procurement, deliberately. Four tests pin it (PayoutOnboardingSeedTests). RESIDUAL, operational only: the provisioner runs at entity creation, so an entity provisioned BEFORE 681456f gets no ladder retroactively and there is deliberately no blanket backfill - such installs still need `seed_payout_approvals --platform --all-tenants` run ONCE, and nothing reports which entities those are (the M18 FRD already records that gap). docs/payments/payment_settlement.md and the module-docs playbook were corrected to match; MRD v2.16 and M18 FRD v1.1 were both checked and already described it correctly, so neither needed a version. ORIGINAL ENTRY: Seed a payout-batch approval template per live entity before go-live (2026-07-12): bulk payout submission is approval-gated only where a `payments.payout_batch` WorkflowTemplate exists for the batch's (school, branch) scope - with no template, a lone `payments.payout.create` holder can `POST /payout-batches/<id>/` (or create-with-`submit:true`) and disburse with no checker (opt-in-by-template, mirrors the finance approval slices). Operational, not a code fix: publish a `payments.payout_batch` template (checker stage, `approver_permission_key=payments.payout_batch.approve`, SCHOOL scope) for every entity that can disburse, before it goes live. See docs/payments/payment_settlement.md §8.1 / §4 and vs_payments/workflow_handlers.py.

# THE FOUR APPROVAL-ENGINE TEST GAPS ARE CLOSED (2026-08-16, commit 66f84f2): 24 tests, no production code touched, and both test files are PURE ADDITIONS so no existing test was modified. (a) The threaded race is IN, not skipped as flaky. It is deterministic rather than timing-dependent: the contender blocks inside a real `SELECT ... FOR UPDATE` on the row the holder locked and cannot finish until the holder commits, so the timing assertion can only fail if the contender never reached the lock, which is a genuine regression rather than a flake. Run 5 EXTRA times beyond the suite (8.5-8.7s each), green every time. Covers two overrides at once AND a repair that commits first denying the concurrent override, which is the property that stops break-glass becoming an approver bypass. (b) Migration 0023's data path, asserted PER DOCUMENT TYPE via subTest because the filter is a literal tuple and a missing type would fail silently forever, plus idempotence and the reverse. (c) ORGANOGRAM stages: the cache must answer 'I cannot say' and leave the memo untouched, an empty climb is still offered to the live resolver rather than written off as unstaffable, and an unstaffed organogram stage PARKS instead of auto-approving. This is the work that turned up the tenant-containment hole now at the top of Undone. (d) The parked and override filters under a branch-scoped tenant, with the caller narrowed through a branch-pinned ROLE GRANT resolved by vs_rbac.scoping.visible_branch_ids and the real permission gate running (nothing touches request.branch, which 6c4bb41 removed). Includes a set-arithmetic partition assertion that fails for ANY widening rather than only the imagined ones, and the branch-optional shape. Verified one app at a time on a unique DB_NAME: vs_workflow 240 (was 233), vs_procurement 485 (was 468), vs_user 105, vs_payments 139 - all OK.

# STALE JWT AND ACTIVATION DOCSTRINGS CORRECTED (2026-08-16, commit 2477279), comments only, no executable line changed. The JWT docstring advertised `school_id` and `user_type` claims that `for_user` does not emit (it emits tenant_id / tenant_slug / branch_id / account_status / full_name), called the ids UUIDs when DEFAULT_AUTO_FIELD is BigAutoField, and named VISION_STAFF, a user_type value that no longer exists. Three more stale references in the same file went with it: the module path, the settings wiring string, and a claim that CustomTokenObtainPairView is 'registered' when no urls.py references it. SWEPT THE CLASS, not the case: `InvitationService.activate` promised to issue JWT tokens and return access+refresh, while the body does neither and returns only a message. DELIBERATELY LEFT: the now-unused CodeXRefreshToken import in invitation.py - whether activation SHOULD auto-issue tokens is a product question, and deleting the import would quietly foreclose it.
# BRANCH-SCOPED ROLES NOW WORK, SHIPPED 2026-08-13 (commit 6c4bb41). Granting a role at one branch used to store fine, display fine, and grant NOTHING — the holder was locked out entirely, not restricted. Two causes, both fixed. (1) request.branch was READ in 13 places and SET in none, so only whole-tenant grants ever matched; 11 of those readers used getattr(request,'branch',None) which is why an earlier grep found only 2. All 13 removed. (2) The SCHEMA forbade it: uq_active_tenant_user_role covered (tenant,user,role) with branch excluded, so one person could not hold the same role at two branches at all. Now two partial constraints — whole-tenant unique per role, branch rows unique per role+branch. A single constraint including branch would have been WRONG: NULLs do not compare equal in Postgres, so duplicate whole-tenant grants would have slipped through. Access and visibility now both derive from vs_rbac/scoping.py:visible_branch_ids (whole-tenant grant → whole tenant; else the union of granted branches; else the caller's own User.branch), memoised once per user per tenant; procurement's _branch_lookups consumes it, so there is one source of truth rather than two. SAFE TO SWITCH ON, verified before behaviour changed: zero assignments carry a branch and zero role templates are branch-scoped, so nothing dormant woke up. Suites re-run on FRESH databases after the final edits: vs_rbac 280, vs_procurement 468, vs_workflow 233, vs_user 105, vs_tickets 35, vs_tenants 28, core 46, vs_finance 519, vs_import_data 7 — all OK; migration graph clean; vs_rbac re-verified green on main after the cherry-pick. Negative control passed both ways (breaking access matching failed the Adebayo/Sunday tests by name; breaking narrowing failed 10 others).

# DECIDED 2026-08-15 by the user, all three left as they are, so no code changes: (1) The approvals queue REPAIRS TENANT-WIDE, not per entity. An inbox is per-user and the repair grants nobody anything - it only restores reachability to a stage that was parked - so narrowing it would leave other branches' stuck work unrepaired for no gain. (2) The PARKED-approval queue is NOT narrowed by branch. Stuck work should be visible so somebody fixes it; hiding another branch's parked request behind a filter is how it stays parked. Note the ordinary approval queue IS branch-narrowed (Round 3) - this decision is about the parked view only. (3) VENDORS ARE NOT BRANCH-SCOPED. Vendor.branch exists but nothing narrows by it; supplier records stay entity-wide master data, consistent with the vendor list. Do not add branch filtering to vendors without a new decision. The third leftover in the same entry - RBAC reading a request.branch nothing ever set - was fixed separately by 6c4bb41.


# CLOSED 2026-08-15 during the pre-sprint todo sweep, each verified in the code rather than assumed: attachments now EXIST on vendor invoices and payments (VendorInvoiceAttachment models.py:1792, VendorPaymentAttachment :1985), so the attachment gap is closed; school decoupling phase D shipped (ddacb64); Round 3's two known gaps are both closed - the analytics reports are branch-coherent (69a91c7, 68 branch_scope references in reports.py) and RBAC no longer reads the never-set request.branch (6c4bb41 removed all 13 readers; permissions.py:191 is now only a comment recording that it was never set); the FAL spec v1.1.1 correction was a record of finished work, not a task; and three notes - the path-equivalence negative-control method, the worktree venv trap, and the shared-test-database rule - now live in CLAUDE.md and AGENTS.md where sessions actually read them. The two procurement DECIDED entries and their build list are superseded: rounds 1-4 all shipped (2d3c4c1, 8d78598, 770e267, 8893c2c).


# THE DJANGO ADMIN IS GONE, AND TESTS RUN ON POSTGRES (2026-08-15, both user decisions): (1) ADMIN. All 13 admin.py files deleted and `django.contrib.admin` removed from INSTALLED_APPS, along with the dead `from django.contrib import admin` import and the commented-out `path("admin/", admin.site.urls)` in apps/urls.py. Verified safe before deleting rather than assumed: no module imported any app's admin.py, no migration declared a dependency on the "admin" app, and the only non-registration code in all 13 files was a one-line display helper in vs_todo for a page that was never served. A fresh Postgres database migrates clean with zero django_admin_* tables. NOTE what stays behind in EXISTING databases: an empty `django_admin_log` table and three `admin` rows in django_migrations. Harmless while empty, but if that table ever had rows, deleting a user would now hit an FK violation - the Python collector no longer knows about LogEntry because the app is not installed. Drop it when convenient: `DROP TABLE IF EXISTS django_admin_log; DELETE FROM django_migrations WHERE app='admin';`. `django.contrib.messages` is still installed and nothing imports it (it was there for the admin) - a candidate for the same treatment. (2) TESTS ON POSTGRES. apps/settings/test.py was SQLite and is now Postgres, mirroring ci.py, with DB_NAME still selecting the database so parallel sessions do not collide. TWO separate problems were making local runs lie: SQLite (threaded tests, select_for_update, the finance audit trigger and the branch-code allocator all behave differently or not at all - README even admitted the full migration chain does not run on it), and NO EAGER CELERY, which meant every test that dispatches a notification, export or webhook needed a live Redis broker just to enqueue and errored with a connection failure unrelated to the code. ci.py had both settings right all along, which is why CI was green while a local full run showed 78 errors and 15 failures.

# SUPERSEDED SAME DAY by the entry above this line in spirit - read this one for the inbox work, but note the template design CHANGED after user review: the HTML is now STORED per template (migration 0006) and editable from the console, with html_is_custom deciding whether a row still follows the shared layout. The "no markup in the database" design described below lasted one review cycle; it was correct about consistency and wrong about who needs to see the markup. Also note the escaping consequence: stored markup is rendered as a Django template, so render_template now takes autoescape and the HTML path passes True - values must be escaped into markup, and that is a security invariant with a test on it.

# NOTIFICATION TEMPLATES NOW CARRY NO MARKUP, AND THE INBOX OPENS ON UNREAD (2026-08-15): three requested changes, all in vs_notifications. (1) TEMPLATES. A template was subject + body + a hand-written HTML document, so only 5 of ~60 emails had any HTML at all and the other ~55 shipped as naked text; the 5 that did were 400 lines of `<table>` markup pasted into constants, with `{% if %}` inside style attributes. Now a template is plain text plus an optional button (`cta_label` + `cta_url`, migration 0005), and ONE shared shell in `services/layout.py` composes the visual - it reads the already-rendered body and infers structure from how the text is written (ALL-CAPS line -> section heading, a run of `Label: value` -> details table, `- ` -> bullets, `━━━` -> rule), escapes everything through `urlize(autoescape=True)` at that one boundary, and appends the button. `html_body` survives as an escape hatch that REPLACES the shell; migration 0005 clears it for the five platform-authored templates (invitation, password reset, credit note, debit note, vendor PO) so they render through the shell, and `seed_notification_templates --overwrite` is the way to resync defaults. Net effect: every email is branded HTML, and the ~55 text-only ones improved without anyone writing markup. (2) THE ACTUAL VISUAL. `GET /v1/notify/templates/{id}/preview/` needs no payload: `services/preview.py` scans the template for its own `{{ names }}` and fills believable sample values, so the editor shows the finished email on open; POST still overrides any subset. The response also carries `variables` and `uses_custom_html`, which retires the hand-maintained variable list. HTML comes back as a JSON string, not an HTML response, so the console renders it in a sandboxed iframe and nothing can execute against the API origin. (3) INBOX ORDER + SEARCH. The feed is now ordered (is_read, -created_at, id): unread first, and the `id` tiebreaker is load-bearing - dispatch bulk-creates a batch with IDENTICAL created_at values, so `-created_at` alone let the database order ties differently per query and rows repeated or vanished between pages. That, plus the total absence of a server-side `?search=`, is the whole "pagination breaks when you search" report: the frontend was filtering the fetched page, so every page count described the unsearched list. `?search=` now filters the QUERYSET on subject/body/event label (feed), plus recipient email on history, where it also counts as the required filter. Frontend contract: docs/frontend/2026-08-15-notifications-inbox-and-templates.md (tabs are Unread | Read | All, All last). 66 vs_notifications tests green (24 new).

# The three NON_PO_BLOCKED tests are fixed, and the cause was fixture duplication (2026-08-14): vs_procurement had been red on main since the non-PO control was switched on. `allow_non_po_invoices` now defaults to FALSE (migration 0027) because nothing three-way matches a bill with no purchase order - no ordered quantity, no receipt, no agreed price - so approval is its only control. That default is correct and was NOT changed. THE TESTS WERE STALE, not the code: they post non-PO bills as fixture convenience while testing something else entirely (AP aging isolation, spend/vendor-performance entity scoping, the branch-scoped payment picker). ROOT CAUSE, and why only three failed: seven different places in tests.py build a procurement-capable entity - `build_p2p`, three `_second_entity`, one `_other_entity` and two `build_books` - and when the default flipped only `build_p2p` was given the opt-in. The three that broke were the ones whose SECOND entity posted a bill; the rest passed by luck of never billing through the hand-rolled entity. Fixed at the choke point: a single `_P2PFixtureMixin.allow_non_po_bills(entity)` that every builder now calls, so the next policy change is one line instead of seven. Deliberately NOT applied blanket-wide to every entity: `NonPoInvoicePolicyDefaultTests` builds its own bare entities and pins that the default is off, that the resolved policy agrees, and that a tenant can still turn it on - those three still pass, so the product default is still guarded. No negative control was needed beyond the failure itself: the 'before' state WAS the neutralised state, verified earlier against a stashed clean tree. vs_procurement 445 tests green, the whole suite passing for the first time since the flip.

# TICKET WORKLOAD COUNTERS COUNTED FINISHED WORK (2026-08-14): reported from the frontend as "I cleared my assigned tickets and the dashboard still shows them", and it was ours, not a cache. `vs_admin_console/overview.py::_tickets` counted `Q(assignee=user)` with NO status filter, so every ticket ever assigned to a person kept counting - resolved and closed included - and the only way to clear the row was to un-assign yourself. The line above it had the mirror-image error: `open` counted `status=OPEN` alone, so picking a ticket up (ASSIGNED / IN_PROGRESS) dropped it out of the workload. THE SAME QUERY SHAPE, SAME BUG, in `vs_tickets/views.py::TicketDashboardView` (`assigned_to_me`, `requested_by_me`), which is what the Support screen's own KPI reads - so this was fixed as a class, not as the one reported instance: `ACTIVE_TICKET_STATUSES` in vs_tickets/constants.py is now the single definition every person-scoped workload number filters by, with the reasoning in a docstring so the next person does not "simplify" it back to OPEN. `total` and the by_status/by_priority/by_category breakdowns stay whole-population - a per-status count that filtered by status would be nonsense. The overview key was renamed `open` -> `active` rather than silently changing what `open` means; its only consumer is console-fe, updated in the same change. Also added `?state=active` and `?assignee=me` to the ticket list so a dashboard card can land on exactly the rows it counted (`me` resolves from `request.user`, so it can only ever narrow the caller's own visible set - pinned by a test that another user's queue stays unreachable through it). 70 tests green including 5 new ones.

# LEFTOVERS AUDITED AND MOSTLY ALREADY FIXED (2026-08-14): went through the Round 1, 2 and 4 leftovers and the 2026-08-12 branch-reference note ONE BY ONE IN THE CODE rather than trusting the notes, which is what the notes deserved - three of nine items were already closed and I had just rewritten them as open. (1) THE BRANCH ORACLE IS FIXED: vs_rbac/serializers/tenant.py now resolves every reference INSIDE the tenant and returns one shared BRANCH_NOT_FOUND message, so a foreign branch and a missing branch are indistinguishable; the file carries a comment block explaining exactly that enumeration risk. (2) ROUND 1 (c) IS FIXED: the private `_users_with_permission` no longer exists at all and approval_parking imports the public `vs_workflow.services.parking`. (3) ROUND 1 (a) IS STALE, not open: approval stages no longer name permission keys, they name a ROLE (`approver_source: ROLE`), and `workflow_role_coverage` reports any tenant whose role has nobody in it - the sentence described a system that no longer exists. WHAT I THEN FIXED: a repaired approver was never told. A parked stage is already ACTIVE, so its activation notification fired to an empty recipient list and could never fire again; when somebody was finally appointed, `parking._repair_one` filled the frozen snapshot in silence and the audit row was the only trace, so the person who could now approve found the document by opening the queue on spec - possibly days late. It now sends the same `workflow.stage_activated` event to the newly eligible, and `routing._notify` was promoted to a public `notify` so parking is not reaching into a private name (the very complaint Round 1 (c) had raised). Proven by negative control: neutralising the notify call fails the new test by name. ALSO: added the override coverage for the three document types the suite never exercised - purchase order, vendor bill and vendor payment - since the release service is deliberately type-generic but only requisitions ever held it to that, and those three are the ones that commit and move money, each reaching the ledger by a different amount field. AND ran the vs_finance suite Round 4 skipped when another session held the test DB: 516 tests, green, so that note is closed on evidence rather than assumption.

# Render check DONE 2026-08-14, and it earned its keep: the page-by-page pass caught a real formatting defect that every structural check had passed. `set_cell_lines` cloned a cell's FIRST paragraph as the template for extra lines, and in a NEEDS ATTENTION cell that first paragraph is the bold blue heading - so every bullet I added to Module 20's block rendered bold blue while Module 19's next to it stayed regular. Same bug hit the M20 FRD's resolved and further-gaps blocks. Fixed by reusing paragraphs positionally and cloning the LAST one for overflow, then all three documents were rebuilt from the pristine originals and re-rendered. Also caught by the same pass: both FRD COVER pages still said Version 1.1 after the Document Control table had been updated. HOW TO RENDER ON THIS MACHINE: Word cannot open files via AppleScript at all now (it returns a blank Document1, and the untouched originals fail identically), but it CAN export a document the user has opened by hand - `save as document "<exact name>" file name ... file format format PDF` leaves the window pointed at the .docx, verified via `full name` before and after, so it is non-destructive. Check `saved of document` before closing anything. qlmanage renders the MRD but hangs forever on the FRD family including untouched originals. Verdict on the three documents: 50 pages checked, footers sequential, no clipping, no empty pages, no field errors. FR-011 and FR-030 each split across a page boundary, which is house behaviour - five pre-existing M07 blocks and M20's FR-007 split the same way - so it was deliberately left alone rather than given keep-together settings the documents do not use elsewhere.

# FIXED 2026-08-12 (commit 12fcb87) — user-with-branch creation, and four more of the same class found by sweeping. Root cause: branch and school were declared as UUID references at birth, when School's key was a slug and Branch's an integer; B23 later swept the school half and left branch behind because NO TEST had ever created a user with a branch. Measured old behaviour: a JSON integer worked by accident (DRF coerced it to a UUID and Django coerced it back), the same value as a string 400'd, a real UUID fell out of bigint range, and a foreign branch returned a DIFFERENT message from an unknown one — a cross-tenant id oracle. Sweep also fixed: vs_import_data rollback deleted schools by slug, so EVERY school imported since B23 rolled back to nothing while reporting success; and two raw request values passed into integer columns (vs_config ?branch=, vs_user ?branch_id=/?school_id=) that returned 500 on anything non-numeric. The convention now lives in apps/vs_schools/services/references.py (resolves inside the tenant with an EXPLICIT filter, not the ambient manager — the import path never sets that context), and vs_procurement's helper delegates to it so there is one convention; 402 procurement tests passed unchanged, proving parity. 793 tests green across vs_user/vs_rbac/vs_config/vs_import_data/vs_schools/vs_procurement. LEFT DELIBERATELY: vs_rbac/serializers/tenant.py:201,372 resolve a branch type-correctly and DO enforce tenancy, but their foreign-branch message still differs from not-found, so that narrower oracle survives — worth closing when someone is next in that file.

# ROUND 2 SHIPPED 2026-08-11 (commit 8d78598) — closes gaps 5 and 6. Procurement documents now record a branch. Root cause was that request.branch is read in 15 places but NO middleware or auth class ever sets it (always None); the authoritative source is the effective user's own User.branch FK (apps/vs_user/models.py:126). Bound raiser gets it by default and cannot raise for another branch; unbound raiser may name any branch in the tenant or leave it empty (a genuine entity-level purchase, never an error). Downstream documents inherit from their SOURCE, never from request input, so a PO cannot be steered away from its requisition; disagreeing sources resolve to entity level. Lists expose + filter branch and narrow to a bound user's own, always inside the entity filter so branch can only narrow. No migration needed (column already existed on every document). Verified: per-branch approval TEMPLATES now resolve for free, since vs_workflow/services/submission.py:50 already cascaded on document.branch. All 292 pre-existing procurement tests passed unmodified, which is the evidence the branchless shape is unregressed. FOLLOW-UPS: (a) BIGGEST — detail and action endpoints (GET/PATCH /requisitions/<pk>/, submit, post, match...) are still entity-scoped only, so a branch-bound user who guesses a pk can read and act on another branch's document; deliberately left whole rather than half-done. (b) summary/KPI endpoints (/requisitions/summary/, /purchase-orders/summary/) and the procurement dashboard/reports remain entity-wide, so a branch-bound user sees totals that do not reconcile with their own list. (c) RBAC still scores permissions with branch=getattr(request,'branch',None), i.e. always None (apps/vs_rbac/permissions.py:181,255) — deliberately untouched as it is RBAC's shared contract, but it means request.branch must never be trusted as scope. (d) whether the parked-approval queue should also be branch-narrowed, now that Round 1 and Round 2 are both in.

# ROUND 4 SHIPPED 2026-08-11 (commit 770e267) — closes gaps 3 and 4. Audited override releases a PARKED approval: new create-only ApprovalOverride side table (actor, amount at the time, stage, reason), permission procurement.approval.override seeded but in NEVER_GRANTED_BY_DEFAULT so nobody holds it until deliberately assigned, FinanceAuditAction.SPEND_APPROVAL_OVERRIDDEN, endpoint POST /v1/procurement/approvals/<workflow_id>/override/ (all four approvable doc types), serializer field approved_by_override + ?approved_by_override= filter. Releases ONE stage, not the ladder: a later stage with real approvers goes back to ordinary review, a later parked stage needs its own override. 404 tests green in the agent's worktree. FOLLOW-UPS: (a) DECISION NEEDED — HasRBACPermission short-circuits for xvs_super_admin, so a Vision super admin can override without holding the key; can_override() mirrors that deliberately so view and service agree. If the rule is 'nobody, not even super admin', it is a one-line change in can_override plus removing the bypass on that view. (b) an overridden document's approval notification says it was approved by 'the system', because vs_workflow._terminate_approved builds it from the last APPROVED action row and an override deliberately writes none — cosmetic, notification-only, audit trail is correct; fixing it means passing an actor through shared engine code. (c) reuses the private helper vs_workflow.services.routing._skip_stage (flagged, same precedent as Round 1's _users_with_permission) and reuses STAGE_SKIPPED_NO_APPROVER / APPROVER_ACTED audit tokens with explicit context markers, because adding AuditEventType members would mean modifying the shared engine. (d) uncovered: a threaded concurrency test (two simultaneous overrides, or override racing the repair), the endpoint end-to-end for PO/vendor invoice/vendor payment (service is type-generic, only requisition is exercised), and the filter under a branch-scoped tenant. (e) vs_finance suite was not run by the agent (another session held the test DB).

# ROUND 1 SHIPPED 2026-08-11 (commit 2d3c4c1) - closes gaps 1, 2 and 9 of the build list below. Remaining Round 1 follow-ups: (a) OPERATIONAL, BEFORE ANY LIVE USE: procurement.approval.approve / approve_senior must be seeded AND actually assigned to someone in every tenant, or every procurement document parks on day one - what used to be silent self-approval is now universal parking; (b) a repaired approver gets NO notification (routing._notify is engine-private, only fires from _activate_stage and terminal transitions) - they only discover the work by opening the queue; fix alongside the override round; (c) approval_parking imports approvers_service._users_with_permission, an underscore-private name from vs_workflow - chosen over duplicating the scope mapping (which would drift), but worth promoting to a public helper in vs_workflow; (d) the approval queue calls repair_workflows(tenant=...) which sweeps the whole tenant rather than the selected entity - deliberate (an inbox is per-user and the repair confers nothing) but wider than the entity boundary the rest of that view uses; (e) uncovered by tests: parking on POs/vendor invoices/vendor payments (only requisitions expose is_parked), the migration's own data path, a real threaded race on the repair, and ORGANOGRAM-source stages which take the un-memoised live path.

# ROLE COVERAGE CLOSED, with one honest caveat (2026-08-13): the permission-to-role move repointed every central stage at a role KEY and no tenant had the roles. `workflow_role_coverage --create` made all 4 (creating a role grants nobody anything, which is the point), then both tenants were staffed and the command now reports 'Every tenant can staff every central approval stage'. codex: Remi Afolabi and Uche Obi hold BOTH `procurement-approver` AND `procurement-senior-approver`. BOTH roles to BOTH people on purpose - splitting a two-person bench deadlocks, because a requisition raised by the sole procurement-approver cannot clear its own manager stage (a requester may never approve their own submission) and parks with nobody able to release it. The two were picked ARBITRARILY: dev's organogram is engineering-only (7 positions, no finance or procurement seat), so there was no signal. Reassign freely. demo-academy: had exactly ONE user, which made role assignment pointless there - that person was necessarily the requester of everything, so every document parked no matter what they held. Now THREE: principal@demo.test plus new bursar@demo.test and registrar@demo.test (SCHOOL_ADMIN on the main branch, mirroring the existing user exactly, NOT superusers so they prove the gate rather than bypassing it; dev password Demo@123456). All three hold both procurement roles. Verified the way that actually matters, by excluding each person as requester in turn: whoever raises the document, 2 eligible approvers remain, on both stages. The coverage command could not have caught the one-user problem - it checks that roles have holders, not whether a holder can ever be somebody other than the requester, which is the property that makes a ladder clear anything.

# Procurement reports now branch-coherent, SHIPPED 2026-08-12 (commit 69a91c7): every analytics report (spend analysis, AP aging, cash requirements, GR/IR aging + drawers, vendor performance, cycle time) answers under the caller's branch, so a branch-bound caller's report reconciles with the documents they can open. Scope resolves once in views/base._branch_lookups into path-free lookup pairs; _branch_q renders it for single-model queries, _BranchScope.q(prefix) renders the SAME resolved answer across relation paths (payment__, grn__, po_line__purchase_order__), so a report cannot compute its own answer. procurement_dashboard moved onto the same branch_scope contract. NOTE Round 3's claim that these already had branch kwargs was OVERSTATED — only spend_analysis had one (branch_filter, for the dashboard) and its view never passed it. SECURITY FIX FOUND HERE: grir_grn_detail and grir_po_line_detail resolved on entity+pk alone, so a branch-bound caller could read another branch's receipt or PO line by guessing an id; both now answer identically to a missing row. GL DECISION: grir_aging's control_balance/difference come from the general ledger, which has no branch column, so they are WITHHELD (null) for a narrowed caller rather than compared against a narrowed receipt walk — doing that would report a fake discrepancy on every read and bury the real 'a posting bypassed the subledger' alarm. HISTORICAL ROWS: reports carry unassigned_excluded_count (a COUNT, never an amount — an amount would disclose head-office spend), absent rather than null when the caller is not narrowed; no row is ever backfilled with an invented branch. Left entity-wide by design: reconcile_ap and grir_balance (their subject IS a GL control account), stock reorder/valuation (StockItem/StockMovement have no branch column at all). Tests: +484 lines, 0 deletions; 481 procurement+workflow and 546 finance+payments green; negative control with the scope neutralised fails 15 of 20, proving the tests bind to branch behaviour rather than fixture values.

# SCHOOL DECOUPLING phases A+B SHIPPED 2026-08-12 (commit 2c09191), rebased onto the branch-lifecycle fix (03601bf) and landed after it. 438 tests green across vs_schools/vs_user/vs_tenants/vs_rbac/core, migration graph clean. Branch now carries its own tenant (backfilled from school.tenant, which is a non-null OneToOne so no row can be left null), plus the two constraints the docstring always promised and never had — unique code per tenant and a single main branch — keyed on tenant so they survive the Phase D model move without a second DDL pass. Fixed a live race: allocate_next_code locked the branch rows, which locks NOTHING when a tenant has no branches yet, so two concurrent first-branch creates both produced code 1; it now locks the tenant row, and reads unscoped rather than through the ambient tenant manager. Dead code removed: _resolve_school, the unreachable _job_school_id path, and two reconcile_tenants checks querying columns the tenant refactor dropped (the command was dying on the FIRST of them, so the second was never reached — the scope doc only knew about the second). CORRECTION TO THE SCOPE DOC: MariaDB was retired 2026-06-12 (apps/apps/settings/local.py:37), so a partial unique index IS portable across everything this repo runs; the doc's R1 recommended a weaker save-only guard based on the stale B17 note. Migration also had to call connection.check_constraints() after its data repair — on PostgreSQL the repair leaves deferred trigger events queued and ADD CONSTRAINT then fails for the rest of the transaction, which a clean-data run never reveals.

# SCHOOL DECOUPLING PHASE C SHIPPED 2026-08-13 (commit 8d82460), 17 of 19 files. Every branch.school.tenant traversal now reads branch.tenant directly; the tenant-aware manager loses a join on every branch-scoped query as a side effect. 987 tests green in the worktree, 960 on main after landing, migration graph clean. SIX traversals remain and all are deliberate (verified by hand): the derivation in Branch.save(), the reconcile_tenants invariant check, one comment, and two vs_rbac/managers.py paths for models linked to a SCHOOL rather than a branch, where school__tenant is already the shortest route.

# PHASE C COMPLETE 2026-08-13 (commits 8d82460 + 247ab4b): the vs_tickets remainder landed once that session committed (c188b64). One-line rewrite plus the two guard tests that prove it — neutering the check makes both fail by name. 29 vs_tickets tests green. No traversal remains except the deliberate six (the derivation in Branch.save, the reconcile_tenants agreement check, a comment, and the manager paths for school-linked models where school__tenant is already the shortest route).

# ROUND 3 SHIPPED 2026-08-12 (commit 8893c2c) — closes gaps 7, 8 and 10, plus Round 2 follow-ups (a) and (b). All four rounds verified together in main: 461 tests green, no migration drift. (1) SECURITY: detail/action endpoints were entity-scoped only, so a branch-bound user could read/act on another branch's document by guessing a pk — now every read goes through views/base._document_or_404 built on a single Q helper (_branch_q) shared by lists, single documents, KPI aggregates and the dashboard, so they cannot drift apart again; another branch's document is byte-identically indistinguishable from a missing one. (2) TENANCY: publish_default_templates wrote tenant=None, so ALL tenants shared one ladder AND any tenant admin could rewrite everyone's thresholds via /approvals/default-templates/ — rules are now per tenant (service + endpoint + `seed_procurement_approvals --tenant <slug>|--all-tenants`), seeded deliberately BLOCKED with no approver appointed; the platform ladder stays as a last-resort fallback, only safe because Round 1 made it park. Idempotent, never clobbers a tenant's customisation, no backfill (a blanket one would be wrong: seeding blocks a tenant until it appoints someone). (3) ROUTING: verified in vs_workflow/services/approvers.py that `branch_arg = branch if scope == BRANCH else None` — so PLATFORM and SCHOOL are IDENTICAL in effect (both resolve tenant-wide holders only) and a branch-assigned approver was never routed anything. Both stages now BRANCH; migration 0025 moves already-published procurement stages still on PLATFORM (filtered to the four procurement doc types, leaves admin-customised scopes alone, reversible). NULL-BRANCH DECISION: an entity-level document resolves to tenant-wide holders ONLY and parks if there are none — a purchase for the whole entity is no single site's to approve; and because BRANCH with a null branch forwards None, it produces byte-identical SQL to the old PLATFORM behaviour, which is what makes a branchless tenant provably unchanged. (4) COVERAGE: new approval_coverage.py answers who can approve at each branch and where nobody can, via a new PUBLIC alias vs_workflow.services.approvers.users_with_permission (verified purely additive — delegates to the private one, no behaviour change), which also pays off Round 1 follow-up (c). (5) TOTALS: summary/dashboard now agree with the caller's visible rows. No existing test was changed (tests.py diff is 1017 insertions, 0 deletions).

# INTEGRATION NOTE (2026-08-11): Rounds 1, 2 and 4 verified TOGETHER in a clean worktree — 422 tests green, no migration drift. Only one merge conflict arose (RequisitionSerializer.Meta.fields, where all three rounds added fields); resolved by keeping is_parked + approved_by_override + branch_id/branch_name. Round 2 was built from a base predating Rounds 1 and 4, so anything building on it should start from main, not from that branch.

# Clean the working tree before Aug 1 (2026-07-28): uncommitted changes in apps/vs_finance/{posting,tests,urls,views}.py and apps/vs_rbac/permissions.py, unchanged since at least this morning - finish, commit or stash them; don't open the sprint on a dirty tree.

# Small waivers post again, and the gate now asks the engine's question (2026-08-14, from the frontend's reply doc `docs/frontend/2026-08-14-frontend-reply-approval-gate.md`): a ₦2,000 goodwill concession was refused at `/post/` exactly as a ₦400,000 one was, so the configurable threshold never bit and there was NO route to the ledger for a small waiver - submitting it would have entered an instance where every stage is skipped and terminated APPROVED with nobody looking. TWO causes, and fixing either alone would have changed nothing. (1) THE GATE ASKED THE WRONG QUESTION: `approval_required` decided on template EXISTENCE, which is only right while a ladder's first stage is unconditional. It now asks *would any stage of the resolved template actually apply to this document*, walking the template with the engine's own route resolution and `evaluate_condition`. This is the class fix - any future all-conditional ladder had the same bug waiting, and journals, payouts and procurement escaped it only by the accident of an unconditional first stage. (2) THE SEEDED LADDER'S FIRST STAGE WAS UNCONDITIONAL, so *something* always applied and no gate implementation could have let a small waiver through; `_stages_payload` now puts the threshold on both stages. Above the threshold nothing changed at all - same two stages, same approvers. NEW SHARED CHOKE POINT `vs_workflow/services/resolution.py` (`document_scope`, `resolve_template`, `template_requires_approval`): the gate and `submit_for_approval` had hand-copied cascades and now share one, which incidentally closed two more disagreements nobody had hit yet - the gate ignored `is_active` (a tenant that switched its own ladder off was gated by a template the engine would have fallen through) and ignored the template `code` (a ladder under another code gated documents the engine would never route through it). MIGRATION 0021 repairs tenants seeded before this, since the seed is deliberately non-destructive and would never revisit them; it rewrites ONLY a ladder still in the exact seeded shape (first stage `approver` with no condition, senior carrying the seed's exact `gte`), so anything an admin edited is left alone, and it is reversible in the same narrow way. FRONTEND REQUEST 2 SHIPPED: read-only `approval_required` on all four adjustment serializers AND on the hand-built `ar-adjustments` rows - the frontend correctly spotted that the serializers alone would miss the one screen the Post button is actually on. Cost controlled by a request-scoped `ApprovalGate` that resolves per distinct `(document_type, tenant, branch, code)` and evaluates conditions in memory, plus `select_related("entity__tenant")` on five querysets, since the gate scopes through the ledger entity's owning tenant and would otherwise fetch that pair per row; two tests assert ten rows cost the same queries as one. WHY THE OLD TEST MISSED IT: `test_a_waiver_is_gated_only_above_the_threshold` asserted the STAGE STRUCTURE and never called the endpoint, so its name described behaviour that never happened - new `AdjustmentThresholdGateTests` goes through the endpoint or the gate every time, including the invariant that matters (a document the gate lets through must not stop at an approval stage when submitted, and one it gates must). Reply written to `docs/frontend/2026-08-14-backend-reply-approval-gate.md`; the frontend's four smaller points were all checked and are all brief errors, not code defects, so nothing changed for them. MRD v2.14, M20 FRD v1.2 and M07 FRD v1.2 written and reconciled (M20 gains the approval-requirement read, M07 the shared predicate; 408 to 410 entries). The FRDs had DOCUMENTED the threshold behaviour while the code refused every concession, so the revisions correct the record rather than describe a new feature. Render check done: 50 pages across the three documents, and it caught a bullet-formatting defect plus two stale cover version labels that the structural checks had passed.

# THE PAYOUT GATE IS ON (2026-08-13): ran `seed_payout_approvals --platform --all-tenants` for real after a dry run. Three templates now exist for `payments.payout_batch` - the platform fallback plus codex and demo-academy - each one stage, ROLE-sourced on `payout-approver`, skip_if_no_approvers=FALSE. Before this, `approval_required(batch)` was False everywhere because NO template existed, so a lone `payments.payout.create` holder could push a whole batch to the bank with no checker. That door is now shut on both tenants. Dry run first, and it mattered: I expected demo-academy to FAIL, because publish_template refuses a ROLE stage naming a role the tenant lacks and demo-academy had no `payout-approver`. It passed because `ensure_tenant_approval_templates` creates the role HOLDER-LESS first, deliberately, so seeding works on a fresh tenant without inventing approval authority. STATE NOW: codex has 2 holders (admin@codexng.com, payout.approver@codexng.com) so its gate is fully working - either can check the other, neither can self-approve. demo-academy has the role and ZERO holders, so its payout batches will PARK, which is the safe failure the whole design turns on: not a silent pass, and the no-approver dialog offers a recorded way through. `workflow_role_coverage` now picks the payout role up automatically (the platform fallback is a central template) and reports demo-academy as the one gap. 0 payout batches existed on dev at the time, so nothing in flight was affected. NEXT: assign a demo-academy payout approver, or leave it parking deliberately.

# workflow_role_coverage now catches the sole-holder trap it used to call healthy (2026-08-13): the command answered 'does this role have holders', which is NOT the question it exists to answer. A role held by exactly ONE person reads as staffed and is not: a requester may never approve their own submission, so everything that person raises has ZERO eligible approvers and parks with nobody able to release it. This was not hypothetical - demo-academy was reported GREEN while holding exactly that state, one user holding both procurement roles, and nothing there could ever have been approved. The command now reports two distinct failure modes and explains why they differ: NO HOLDERS is the dangerous one (resolves to nobody, and an auto-skip stage passes the request through unapproved), ONE HOLDER is the quiet one (parks, which is the safe failure, but a tenant can sit in it indefinitely believing it is staffed). The one-holder line NAMES the person so it is actionable, and is styled WARNING not ERROR because it cannot cause a silent approval. The success message no longer says 'every tenant can staff every stage' unless a second pair of eyes is genuinely available whoever raises the document. 5 tests in new `vs_workflow/tests/test_role_coverage.py`: missing role, zero holders, ONE holder is not called healthy (the regression, asserting both the warning and that the success line is absent), two holders is healthy, and --create makes the role while granting nobody anything. Verified live on dev by suspending two of demo-academy's three assignments, confirming the warning fires and names principal@demo.test, then restoring them.

# The 'order-dependent' health test was a phantom, and the real lesson is about long runs (2026-08-13): `vs_admin_console.tests_overview.OverviewSectionTests.test_health_reports_posture_not_the_whole_command_centre` failed once in a 2162-test run with an extra `'trend'` key, and passed alone and in its own app run. Chased it by reading rather than by bisecting, and the code says it CANNOT fail: `_health()` builds a literal three-key dict, `data['health'] = _health()` is the only assignment and nothing post-processes the payload, `overall_posture()` has no `trend` key, and no test anywhere patches `_health` or `overall_posture`, so there is no leaked-patch mechanism either. THE TELL: the failing traceback rendered source text (`other = make_vision_user(email='ov-other@codex.test')`) that does NOT match line 208 of the file - that email last existed in commit 3bd8fe6. Python's linecache had read a DIFFERENT version of tests_overview.py than the bytecode that ran, which only happens if the file changed underneath the run. Confirmed by re-running the whole suite pinned at d094f93 with the tree verified stable: 2162 tests, OK, zero failures. NOT A BUG AND NOTHING WAS FIXED - the failure was an artifact of another session editing the working tree during a 47-minute run. THE ACTUAL LESSON, worth more than the test: a full-suite run on this machine takes 47-71 minutes, and a concurrent session editing source in that window produces failures that look like flakiness or test pollution and are neither. The existing OPERATIONAL note covers the shared test DB; this extends it to shared SOURCE. Before trusting any long-run failure, check `git status` and the HEAD sha at start and end - the rerun captured both, which is why this one could be settled rather than argued about.

# PLATFORM_USER_CREATION may auto-skip when unstaffed (2026-08-13, USER DECISION, asked and confirmed): its stage names `xvs_platform_admin` with skip_if_no_approvers=TRUE, and that role had ZERO holders, so every platform user-creation request was passing through with no review. Raised it as a real control question given what that stage creates; the decision is that skipping is acceptable BECAUSE skip_if_no_approvers is true - the flag is the contract, and a stage that opts into skipping means it. No code change. Note the current dev state is not the skipping one: `xvs_platform_admin` picked up exactly one holder (payout.approver@codexng.com) as a side effect of creating that account, so the stage routes to them rather than skipping. Delete that one assignment to restore the auto-skip behaviour.

# Audited EVERY merge in both repos, not just 3ba6bdb (2026-08-12): ran the three-way classification over all merge commits in backend and console-fe, then a line-level second pass (which lines a side ADDED are absent from the merge result), then a size comparison base/local/remote/merge/HEAD to tell real loss from supersession. HEADLINE: NO merge in either repo discarded a side's work wholesale by the file test, and 3ba6bdb is the ONLY merge whose losses survived to today - both already fixed. Per merge: 9fcce22 (2026-05-24) truncated `vs_notifications/views.py` to git's EMPTY BLOB (e69de29b) - 562 lines on BOTH sides became 0, an add/add conflict resolved by deleting; rebuilt afterwards and 777 lines today, vs_notifications green. 4d0d846 (2026-04-10) discarded a LOCAL refactor that had shrunk vs_rbac serializers 839->628 and views 620->509; the merge kept the larger base/remote version. Moot now: serializers.py no longer exists (split into a package) and views.py is 1340 lines. 3759634 (2026-04-12) is mostly both sides generating initial migrations, which is normal divergence not loss, plus reset_db.py where the merge correctly took remote's fuller 342 lines over local's 127 (still 342 today). 6008882, 3ab8f7a and 709cf4d: nothing at line level. console-fe has 5 merges, ONE diverged file (create-permission.tsx) where the merge took the larger local version; no loss. BACKSTOP: full backend suite 2162 tests, one failure, order-dependent and unrelated (see Undone). CONCLUSION: the tree is sound; nothing else needs recovering. The pattern is real but self-limiting - conflicts land in the same few files and get rebuilt - EXCEPT when the lost thing is small and quiet, which is exactly what 3ba6bdb's one-line my_queue deletion was.

# Audited merge 3ba6bdb for anything else it reverted (2026-08-12): METHOD, so it can be repeated on the other merges. Take the merge's two parents and their base (`git rev-list --parents`, `git merge-base`); for every file changed on a side, classify the merge result as (a) identical to that parent = kept, (b) identical to base = the side's work FULLY DISCARDED, (c) identical to neither = DIVERGED, the only place partial loss can hide. File-level, so it is exhaustive: anything not diverged is provably intact. RESULT: nothing fully discarded on either side; exactly 6 diverged files, the conflict set. Two were real losses, both already fixed this session: `vs_admin_console/overview.py` (both `_approvals` and `_submissions` reverted to count-only while their tests were kept) and `vs_workflow/services/my_queue.py` (the parking repair call deleted, import left behind). The other four are legitimate supersession by the permission-to-role rename and were checked line by line rather than assumed: `vs_workflow/views.py` (my continue-without-approval endpoint and approval_block both survive; the removals are all the school-to-tenant scoping rename), `vs_workflow/services/approvers.py` (my UnknownApproverSourceError guard survives, 3 refs + the exception class), and `vs_procurement/approvals.py` + `constants.py` (thorough rename, docstrings updated with it). Remote side lost nothing outright, and `3ba18f8` had already repaired the half-applied parts. FULL BACKEND SUITE AS THE BACKSTOP: 2162 tests, ONE failure - `test_health_reports_posture_not_the_whole_command_centre`, which PASSES alone and passes in its own app run (104/104) and only fails in the 2162-test ordering. Order-dependence in the health section, pre-existing, unrelated to the merge and to anything changed here. Left alone; see Undone. THE UNCOMFORTABLE PART: the my_queue loss was a single deleted line with the import left in place, and while a test did fail, it failed pointing at the tenant/school signature change, which masked the real cause - I found it by reading the file, not from the failure. There are 8+ merges of this exact shape in recent history and only this one was audited.

# Restored the overview worklist the merge reverted (2026-08-12): `3ba6bdb` had reverted BOTH `_approvals` and `_submissions` in `vs_admin_console/overview.py` to their count-only versions while KEEPING the tests from `51e7c40` and `75d1d76`, leaving 6 red tests on main (KeyError 'items', KeyError 'delegated'). Recovered the implementations from `75d1d76` rather than rewriting them from the tests, so the original reasoning came back intact: `_approvals` sorts OLDEST-FIRST before applying the cap (the queue screen is newest-first; inverting it here is deliberate, so a lingering decision cannot hide below the cap while fresh arrivals fill the visible rows), reuses the snapshot list `pending_approval_count` already materialises so the rows cost nothing extra, and resolves requesters AND delegation principals in ONE `User.in_bulk`. Plus `delegated` (how many of the caller's pending decisions they hold as somebody's delegate) and `on_behalf_of_name` per item, which is what lets the dashboard split delegate cover into its own box. `_submissions` regained its RETURNED_ITEMS_LIMIT-capped items. CARE TAKEN: 75d1d76 sits on a lineage that also carries the schools->tenants rename (vs_tenants.models, platform.tenants.view, _tenants()), which main does NOT have, so the restore was surgical - only the two function bodies and the two limit constants, verified afterwards by grepping my own diff for any schools/tenants rename contamination. vs_admin_console 104/104 green, was 98/104. That merge has now been caught reverting work in three places (the parking repair call in my_queue, and both of these) while keeping the tests that prove the work existed - worth auditing what else it touched.

# The admin overview crashed on every load, found while fixing a parameter name (2026-08-12): asked only to rename a stale `school` parameter in `vs_admin_console/overview.py`, which turned out to be hiding a live NameError. `_submissions` had been renamed to take `tenant`, but its BODY still read `if school is not None: qs.filter(tenant=school.tenant)`, and nothing in the module binds `school` - so the overview endpoint raised `NameError: name 'school' is not defined` unconditionally. Proved it rather than assumed it: stashed the fix, ran the test, got the NameError; restored the fix, and it advances past. Body now filters on the tenant it is given. `_approvals` renamed too (it already received a tenant; only the parameter name still said school, which is exactly what let the bug next door hide in plain sight). Same merge `3ba6bdb` that dropped the parking repair call also reverted `_approvals` to its thin version while keeping its tests, so 6 vs_admin_console tests still fail on KeyError - see Undone, deliberately not fixed here.

# Reconciled the parking/no-approver work with the permission-to-role move (2026-08-12): the user replaced RBAC_PERMISSION with ROLE / WORKFLOW_GROUP / DYNAMIC_ROLE (+ ORGANOGRAM). The safe-by-default structure survived the merge intact and was carried forward correctly by them: `_holder_ids` still memoises only the one source it can answer (now ROLE) and returns None for everything else, and `resolve_approvers` now has a branch per source with the UnknownApproverSourceError else still in place. THE DESIGN PAID OFF ON THE FRONTEND: the whole approver model changed and the only edit the dialog needed was renaming the optional key chip from permission_key to role_key; the backend-composed `requirement` sentence carried every other case with no change. FOUR THINGS THE MERGE LEFT BROKEN, all found by running the suites. (1) THE REPAIR TRIGGER WAS SILENTLY DROPPED. `my_queue.pending_approval_snapshots` kept `import parking` but LOST the `repair_workflows(...)` call - the single line the entire parking generalisation hangs on. Without it a parked document never un-parks when somebody is assigned the role, which is the original bug, restored. Put back against the new `tenant` signature (the function now takes a tenant, not a school). (2) PARKING GOT SLOWER PER ROW, caught by procurement's own query-count test: 27 queries for a 15-row parked page vs 17 for 5 rows. Two causes, both per-stage. `stage_override_for` was called unmemoised inside the resolution cache (the one place that exists to avoid per-stage cost) - now memoised on (stage, tenant). And `instance.tenant` was a LAZY FK the parking scan never fetched, so both the override and role-holder lookups triggered a tenant query per row; `empty_active_stages` and `lock_parked_stage` now select_related instance__tenant/branch plus stage__approver_role/group. Diagnosed by dumping normalised SQL rather than guessing (two wrong guesses first). Now FLAT: 9/9 healthy, 12/12 parked, down from 27. (3) `publish_template` now refuses a stage naming a role the tenant lacks, but the payments fixture only created that role inside `_make_approver`, so 7 pre-existing tests broke. Added `_ensure_approve_role`, called by both publish helpers: the role EXISTING and the role having HOLDERS are different facts, and most of these tests turn on the difference. (4) My own test was wrong, not the code: `stage_role_key` prefers the portable key and falls back to the `approver_role` FK, so blanking only the key still resolved one. Clears both now. 1237 tests green (workflow, payments, procurement, finance); FE 360, tsc + eslint clean. NOT MINE, left alone: `vs_admin_console/overview.py` passes a tenant correctly but still names the parameter `school` (cosmetic, another session's uncommitted file), and the FE has in-flight approver-group work - `ApproverSource` union in workflow-types.ts still reads RBAC_PERMISSION|ORGANOGRAM and a route points at a `workflow/approver-groups` page that does not exist yet (1 tsc error).

# An unknown approver source can no longer silently approve spend (2026-08-12): `resolve_approvers` was `if ORGANOGRAM: ... else: <assume RBAC>`, and that else-branch swallowed every other value - an unrecognised source fell in, found no permission key, and hit `return []`. THREE DEFAULTS THEN LINED UP BADLY: `skip_if_no_approvers` defaults to True on the model (only the money ladders set it False), `routing.advance_instance` skips a stage that resolves to nobody when that flag is True (routing.py:303), and `[]` was overloaded - 'this stage resolves by permission and names none' and 'I have no idea how to resolve this stage' produced the IDENTICAL answer, which downstream code cannot tell apart. Net effect: a template naming a source the engine had not been taught would not park, it would SILENTLY SKIP ITS OWN APPROVAL STAGE and let the document through unreviewed. On a payout batch that is money out with nobody's signature, caused by a config value nothing recognised. This was latent (only 2 sources exist today) but was about to be armed by the approver-model rework. FIX: explicit dispatch with no catch-all. ORGANOGRAM and RBAC_PERMISSION each get a branch; anything else raises new `UnknownApproverSourceError` (follows the existing UnknownConditionFunctionError / UnknownOperatorError / UnknownDocumentTypeError family). A permission stage with a BLANK key still returns [] because that is a legitimate empty answer, not a broken template - the two cases are now distinguishable, which was the root problem. Adding a source now means adding a branch, and forgetting is impossible to miss instead of silently dangerous. SECOND FIX THE FIRST ONE FORCED: raising propagated straight into the parking repair, which runs on EVERY approvals-inbox read over whatever is parked in the tenant, so one team's broken template would have taken the inbox down for everybody. `repair_stages` now guards each stage, logs, and continues. The split is deliberate: the RESOLVER raises at activation where the skip decision is actually made, the background SWEEP degrades to leaving the stage parked, which is the state it was already in. 3 new tests: the unknown source raises rather than skipping (with skip_if_no_approvers=True, the risky default, set explicitly), a blank-key permission stage still resolves to nobody, and one broken template does not break everyone else's inbox. 1216 backend tests green across workflow, payments, procurement, finance and user. NOTE: asked the user where their workflow-approver work lives; it is in neither repo (ApproverSource still has only the two members, approvers.py untouched since 8893c2c), so this fix is against current main and may need rebasing onto theirs.

# Parking and the no-approver dialog no longer assume permission keys (2026-08-12, ahead of the approver-model rework): both new pieces read `stage.approver_permission_key`, and the approver model is moving away from it toward groups / organogram positions / named people. Two silent failures fixed BEFORE the new sources land, because both fail quietly rather than loudly. (1) THE REPAIR WOULD HAVE SKIPPED THEM. `ResolutionCache._holder_ids` special-cased ORGANOGRAM (return None = resolve live) and then treated anything without a permission key as `frozenset()`, which `has_candidates` reads as 'provably nobody', so the repair never even tried. A group-sourced stage would have parked and NEVER un-parked: exactly the defect this module exists to prevent, reintroduced through the new resolution path. The memo is now OPT-IN per source rather than opt-out: only RBAC_PERMISSION-with-a-key is answered from the memo, and every other source, known or not, falls through to the live resolver. An unknown source now costs a query instead of a lost document. A permission stage with a BLANK key also takes the live path now (misconfigured is not the same as unstaffable). Verified `resolve_approvers` does not crash on an unknown source (its else-branch returns [] when there is no key), so the fallback is safe today AND picks up the new sources automatically the moment that resolver learns them, with no change to parking.py. (2) THE DIALOG WOULD HAVE LIED OR GONE BLANK. It hardcoded 'grant someone <key>', which is wrong for an organogram seat and renders empty with no key. `describe_park` now returns `approver_source` plus a `requirement` sentence composed server-side by new `stage_requirement(stage)`, which words RBAC, all four organogram targets, and a truthful generic fallback for sources it has never been taught ('configure an approver for this step on the workflow template') - never an empty string. `permission_key` is now BLANKED unless the stage really is RBAC-sourced, so nobody is sent to grant a key that no longer decides anything. The audit row carries `approver_source` and `requirement` too, so the record survives the model change. FE renders the code chip only when a key is present and the plain sentence otherwise, which means WHEN THE NEW SOURCES ARRIVE THE FRONTEND NEEDS NO EDIT AT ALL - only `stage_requirement` gains a branch. Dialog description reworded from 'no one holds the permission' to 'no one is currently able to approve it'. 4 new tests using a fake future source 'GROUP_MEMBERSHIP': it is repaired not skipped, a blank-key permission stage is not written off, the requirement is never blank and the stale key is not shown, and an organogram stage explains itself in org terms. 1115 backend tests green (workflow, payments, procurement, finance); FE 352, tsc + eslint clean.

# The no-approver dialog verified against the real app, and a double-toast bug found and fixed (2026-08-12): seeded a one-stage payout rule plus draft batches on dev and drove the whole flow in Chrome. The parking condition was real, not contrived: admin@codexng.com is the ONLY holder of `payments.payout_batch.approve` on codex, so submitting as that user excludes the requester and leaves zero eligible approvers. Dialog renders correctly at 1440px and at 390px with ZERO horizontal overflow, both buttons on one row, and it carries REAL data - the live stage label ('Payout approval') and the actual permission key, not placeholders. The seeded template published exactly one stage, which also confirms the one-stage change on real data. The first run accidentally proved more than intended: the Playwright glob `**/continue-without-approval/` silently failed to match (the URL carries a `?tenant=codex` query), so the click hit the REAL server and the full release ran - instance APPROVED, stage STAGE_SKIPPED_NO_APPROVER, audit row `APPROVER_ACTED | RELEASED_NO_APPROVER | 'Ada Admin continued without approval: no approver was available.'` The batch then reached the payout handler and went FAILED at the provider, which is correct on dev (no Paystack credentials); no money moved. BUG FOUND: on the 409 NOT_PARKED path the user got TWO toasts - the hook's reassuring green 'Someone can approve this now, so it has gone for review instead' stacked on the central handler's red 'This is not waiting on an unstaffed approval stage', two messages saying opposite things about the same event. Fixed with `extraOptions: { silent: true }` on the mutation (the house opt-out, same as the notifications bell): this endpoint owns every message it produces, including its own error toast for genuine failures, so exactly one message shows on every path. Re-verified on fresh batches: one toast each. Teardown removed all 5 batches, their instructions, 6 PaymentEvents and the seeded template; workflow instances cascaded (0 payout instances left); login trail scrubbed. FE 352 tests green, tsc and eslint clean.

# Submitting into a workflow nobody can approve now warns, and can be continued (2026-08-12): a document submitted into a stage whose approving permission nobody holds PARKS - submitted, waiting, and with no human able to move it - and the submit screen said nothing, so it looked like it had worked. Two halves. (1) Every submit-for-approval response across all NINE approvable document types now carries an `approval` block (`vs_workflow.services.release.approval_block`) saying whether it parked, on which stage, and WHICH PERMISSION KEY nobody holds, so the warning names the thing an admin would grant instead of saying 'no approver'. Wired into procurement's shared `_approval_response` (covers all four types), vendor payments, payout batches, finance refunds/write-offs/journals. (2) New `vs_workflow/services/release.py` steps past the parked stage and hands control to the engine's own `advance_instance`, so the document terminates exactly as an approved one does (a released payout dispatches, a released PO issues) rather than via a hand-rolled shortcut. USER'S EXPLICIT DECISION, asked and confirmed: the continue is NOT permission-gated - anyone who can submit can click through - and it covers every document type, not just payouts and procurement. What still guards it: ownership checked in the service (`may_release`: the instance's own requested_by, or platform staff - deliberately NOT any authenticated user in the tenant, which would let a stranger release someone else's parked spend); the parked precondition re-checked after a repair pass and again under a row lock, so a reviewer who exists can never be bypassed (409 NOT_PARKED, and the FE reads that as good news: 'someone can approve this now, so it has gone for review instead'); and an append-only audit row naming actor, stage, permission key and reason, written BEFORE the workflow moves. FE: one shared `useNoApproverPrompt` hook + dialog (`no-approver-prompt.tsx`) on five screens, blunt copy - 'Continuing approves it WITHOUT review. Your name and the time are recorded against it.' CONSEQUENCE TO DECIDE LATER: `procurement.approval.override` (CRITICAL, granted to no role, demands a typed reason) is now decorative, since the same outcome is one ungated dialog away at submit time. Left in place and unchanged rather than silently weakened; retiring it is a separate call. Recorded in PERMISSIONS_AUDIT.md as the one state-changing workflow endpoint with no RBAC key, with the reasoning, so it is not rediscovered as a bug. 7 new payments tests (the warning's data, the release dispatching, refusal while an approver exists, a late-granted approver cancelling the bypass, the audit row, the default reason, ownership). 1111 backend tests green across payments, workflow, procurement and finance; FE 346 tests, tsc and eslint clean. NOT DONE: the dialog was never driven in the real app (it only appears on a genuinely parked submission, which needs seeding), so it is type-checked and unit-tested but not visually verified - see Undone.

# Payout approval is one stage, not two (2026-08-12): the seeded ladder was an always-on checker plus a senior stage gated on `total_amount >= 50,000,000` kobo, mirroring procurement. A second signature is only a real control when a second PERSON holds the senior key, and on dev exactly one account held both, so the extra stage bought an extra click rather than an extra reviewer - while doubling the places a batch could park, since neither stage may auto-skip. Now one always-on APPROVAL stage on `payments.payout_batch.approve`, no inclusion condition, so batch size no longer changes who signs. `--threshold` and `--senior-permission` are gone from `seed_payout_approvals`; `WF_DEFAULT_SENIOR_THRESHOLD`/`_PERMISSION` deleted. `payments.payout_batch.approve_high_value` stays REGISTERED but unused by the default rule, so a tenant that genuinely separates signing authority can publish a ladder with that stage restored; nothing was removed from the RBAC registry, so no existing grant was orphaned. Tests reshaped rather than deleted: the old small-batch/large-batch pair became one-approval-releases-it plus an explicit pin that batch size does NOT change routing, so a reinstated condition would fail loudly instead of silently parking large batches on a stage nobody holds. vs_payments 121 green.

# Parked approvals: the repair moved into the engine, where the defect always was (2026-08-12): vs_workflow resolves who may approve exactly ONCE, at stage activation, and freezes it - every read path afterwards consults that frozen snapshot, not live RBAC. A stage that activates while nobody holds its permission therefore freezes an EMPTY list and is unreachable for that attempt: granting somebody the key afterwards changes nothing, because nothing re-asks the question, and only withdraw + resubmit re-snapshots. Procurement hit this and wrote the repair, then fenced it to its own four document types. The engine serves NINE (4 procurement, 3 finance, payout batches, platform user creation), so five types had the identical trap and no repair - including `payments.payout_batch`, the one that sends money to a bank. The logic was never procurement-specific; only the fence was. Moved to `vs_workflow/services/parking.py` with the doc-type filter as an OPTIONAL argument; `vs_procurement/approval_parking.py` is now a thin fence that passes PROCUREMENT_APPROVAL_TYPES and keeps its own approval_state=PENDING pre-filter (an overlay the engine knows nothing about), so every procurement caller, including approval_override's shared lock_parked_stage, is unchanged. THE TRIGGER IS THE REAL CHANGE: the repair now runs inside `my_queue.pending_approval_snapshots`, the choke point BOTH the approvals screen and the dashboard count already share, rather than in one app's view. Each app remembering to call it is exactly how payouts went without it for as long as they did. Scoped to the tenant being read, never system-wide, so one person opening their queue does no work for other tenants. Cost on healthy data: one indexed query returning nothing, measured at 10ms on dev; the plan drives off IN_PROGRESS workflow instances (bounded by in-flight work, not by history) via the existing (tenant, document_type, status) index, and takes zero row locks when nothing is parked. Guarantees unchanged and re-asserted inside the lock: a POPULATED snapshot is never touched (an approver mid-review must not have their eligibility rewritten under them), and nothing is ever approved, advanced or skipped - it restores reachability, a human still decides. 3 new payments tests: the trap itself (a late-appointed approver still gets NotAnEligibleApproverError with no repair), the fix (reading the queue releases the batch, no resubmission), and the freeze guarantee (a second holder appearing after activation is NOT added). Verified across every app that touches the engine: workflow+payments 197, procurement 421 (unchanged by the move), finance+user 581. All green.

# The gate over bulk payouts is now switchable on (2026-08-12): a payout batch pays many beneficiaries at once and is the highest-risk cash-out path in the product. The maker-checker machinery for it was already built (`vs_payments/workflow_handlers.py`, the submit-for-approval endpoint, the FE action on the Batches tab) but the gate is opt-in by template - `approval_required(batch)` is True only where a `payments.payout_batch` WorkflowTemplate exists - and NOBODY EVER SEEDED ONE. Live check before starting: 5 workflow templates on dev, 4 procurement + 1 user creation, zero payouts. So the highest-risk path did not have a jammed door, it had an open one, and one person with the submit key could push a whole batch to the bank unreviewed. New `vs_payments/approvals.py` + `manage.py seed_payout_approvals`, deliberately the same two-stage shape as `vs_procurement/approvals.py` so a tenant holds one mental model for 'who signs off on money leaving': stage 1 always runs on `payments.payout_batch.approve`, stage 2 runs only when the batch total reaches the threshold (default 500,000.00, same as procurement) on `payments.payout_batch.approve_high_value`. Both keys already existed in seed_payments_permissions and were simply unreachable; nothing here creates a key. THE TRAP AVOIDED: the engine's model default is `skip_if_no_approvers=True`, so a carelessly seeded ladder would auto-skip an unstaffed stage and sail the batch through WHILE APPEARING APPROVED - worse than no gate, because now there is a green tick next to money nobody looked at. Both stages force it False, and there is a test asserting it that fails loudly if anyone regresses the seed. Platform fallback upserts (that row is provisioning's to own); per-tenant seeding is non-destructive, so re-running after an admin raised the threshold cannot restore the defaults over them. Divergence from procurement, deliberate: `approver_scope=SCHOOL` not BRANCH, because PayoutBatch.branch is a property that always returns None - the engine forwards None either way, so routing is identical and SCHOOL is just the honest declaration. Also added `PayoutBatch.workflow_amount_field = 'total_amount'` (no migration; it is a class attribute) - what escalates a batch is the money leaving in one run, not any single line. 14 tests including the two that matter: the SEEDED ladder (not a hand-made fixture) refuses a direct submit, and a large batch still moves no money after the first approval and only dispatches after the senior one. vs_payments 118 green, no migration drift. Dry-run validated against real dev data (platform + codex + demo-academy). NOT RUN FOR REAL YET - see Undone.

# Unbooked gateway money now taps somebody on the shoulder (2026-08-12): the failed-webhook queue built earlier the same day made the problem *visible*, and visible is not the same as known - a receipt that failed to book on a Friday evening sat there until somebody happened to open Needs Attention, with the customer's money outside the books all weekend. New `vs_payments/alerts.py` carries two alarms, deliberately different in kind. `unbooked_digest` is a standing daily report, one message per entity (not per event) to whoever holds `payments.webhook.view` there, naming the count, the money it represents, the oldest one, and the reason most of them share - a message someone can act on ('no fiscal period is open') rather than one they cannot ('3 webhook failures'). It re-sends while the backlog exists, which is why it is not per-event: during a month-end close EVERY receipt fails, and a hundred alerts is worth the same as none. `unbooked_surge` is an incident alarm for platform staff holding `payments.unattributed_webhook.view`: several failures inside one hour points at a systemic cause (closed period, provider change) that no single school can fix, and it counts unattributable events too, since those are failures of the same kind and are exactly the ones no entity-scoped screen shows. Both are stateless - the digest reports the current outstanding set, the surge only what landed inside its own window - so nothing is written back onto the event, a missed run costs a late notice and nothing else, and a resolved outage goes quiet by itself. Beat entries: digest 07:00 daily, surge hourly at :05 (window matches interval, so a continuing outage re-alarms once per window). Recipients come from vs_workflow's `users_with_permission` rather than a second copy of the scope rules, and every entity is wrapped so one bad tenant cannot abort the sweep. THE BUG THIS ALMOST SHIPPED WITH: vs_notifications skips a channel SILENTLY when no NotificationTemplate exists for (event, channel) - registering the event type is not enough - so the first live run dispatched to real recipients and delivered nothing at all, with no error anywhere. Four default templates added to the seed, and the tests now assert a Notification row actually lands with real RBAC rows behind it, because a test that only asserts 'the task ran' would have passed the whole way through. Second catch from the same test: the templates said `₦{{ total_amount_naira }}` while format_naira already carries the symbol, so the money read `₦₦400,000.00`. Verified end to end in dev before scrubbing - 2 notifications SENT, both channels, correct subject and body. 12 tests; payments + notifications 148 green. No new permission keys, so no permissions-audit change.

# A vendor advance can now be drawn down from the product (2026-08-12): `allocate_vendor_payment` had no API endpoint - its only caller was the posting path - so a prepayment parked in 1240 could only be applied by a later payment run happening to settle the bill, or by hand in a shell. New `POST /procurement/vendor-payments/<id>/allocate/` behind `procurement.vendor_payment.allocate` (701120, SENSITIVE), the AP mirror of `/finance/payments/<id>/allocate/`, taking either an explicit split or `auto_allocate`. It reuses the draft-plan validator, so a swapped invoice id is resolved against this entity AND this vendor server-side. Finding no eligible bill is reported as the ordinary state it is, not an error. The drawer gains Apply Advance, shown only on a posted payment that actually holds an advance. 8 tests including the 403 and the cross-vendor refusal; procurement 421.

# Fiscal-calendar runway warning (2026-08-12): nothing warned that an entity was about to run out of fiscal periods, and when the last one lapses EVERY posting fails at once - invoices, receipts, payroll, gateway webhooks - because resolve_period returns None for every date. An annual outage waiting on a calendar date, with CODEX and TES both ending 2026-12-31. New `posting.fiscal_calendar_runway` reads how much calendar is left (healthy / expiring within 60 days / expired, with a never-had-one entity counting as expired), the finance dashboard carries it, and a banner says so while there is still time. PeriodClosedError's missing-period message now names the likely cause instead of advising a date change that cannot help. 9 tests; finance 483.

# Virtual-account transfers now book (2026-08-12): a dedicated NUBAN exists so a payer can transfer without a checkout, but nothing matched the resulting webhook - `_find_collection` resolved only against an existing intent and nothing ever set `CollectionIntent.virtual_account` - so the deposit was marked IGNORED and the money sat in the bank and not in the books. The entire virtual-account funding path was unwired, latent only because no accounts were provisioned. A deposit naming a known account now creates the intent its customer would have had and flows through the existing confirm/book path; the provider's own transaction reference becomes ours, which is what makes a re-delivery or replay idempotent. Closed a real hole on the way: confirm_collection fell back to the intent's amount when the provider omitted one, safe for a checkout we priced but not for a deposit built from the payload. 10 tests; payments 82.

# Unattributable webhooks have a platform home (2026-08-12): an event matching neither a collection nor a payout belongs to no entity, so no entity-scoped screen could show it, and it could not be shown to every tenant because the reference belongs to one of them. New platform-scope list and replay behind `IsVisionStaff` (the existing CX-only convention, tenant kind PLATFORM) plus their own keys, surfaced as Health > Provider Webhooks. Replay logic is shared with the entity-scoped view rather than copied. 10 tests, none using the super admin, which bypasses RBAC and would have hidden whether the keys are enforced at all; payments 92.

# Vendor prepayments sit in an asset account (2026-08-12): paying a supplier before their bill exists debited AP for the full gross, putting a debit balance on a liability - the balance sheet asserting that suppliers owe us money. New 1240 Vendor Advances (ASSET), and posting splits at source: Dr AP for what is settled, Dr 1240 for the rest. The mirror of AR's customer credit 2140 with the direction inverted, which is the whole point: cash received early is a liability, cash paid early is an asset. Applying an advance later reclassifies Dr AP / Cr 1240 dated at the later document. Forced along the way: allocation rows needed an effective date (same reasoning as vs_finance 0019 for AR) and reversal had to unwind both journals or 1240 would go negative. Data checked first - 3 posted payments, none with an unallocated balance, so no live journal was posted. 11 tests; procurement 413. See Undone: no endpoint draws an advance down yet.

# Gateway receipts that cannot be booked are now visible and replayable (2026-08-12): when a webhook's booking failed - most often because no fiscal period was open for today - `process_stored_event` marked the event FAILED and swallowed the exception, which is right for the PSP (already acked, retries idempotent) and wrong for us: no endpoint listed `WebhookEvent` at all, so a customer's payment could arrive, fail to book, and exist only as a database row. Fixed a prerequisite bug first - `_dispatch` assigned `event.collection`/`event.payout` AFTER calling the confirm service, so a failure never reached the assignment and left exactly the events an operator needs with no link to anything and therefore no entity; the link is now written before the confirm. New entity-scoped `GET /payments/webhooks/` (defaults to FAILED + IGNORED), `/webhooks/summary/` for the badge count, and `POST /webhooks/<id>/replay/`, behind two new keys `payments.webhook.view` (SENSITIVE) and `payments.webhook.replay` (CRITICAL); `replay` added to the canonical action vocabulary in `seed_actions`. The serializer never returns `payload`/`raw_body`/`headers`/`signature` - provider record, signature material. Replay safety was verified rather than assumed: `confirm_collection` and `confirm_payout` are both `@transaction.atomic`, so a failed confirm rolls back whole and `process_stored_event` no-ops on PROCESSED, meaning a replay can neither double-book nor undo. Console gains Finance > Payments > **Needs Attention** with a per-row Retry, nav-gated by the `payments.webhook.` prefix. 8 regressions including both 403 cases, cross-entity isolation, replay-books-exactly-one, and a replay that still cannot book; payments 72/72 green, verified on desktop and phone with no overflow and no console errors. NOT done, see Undone: unmatched (unattributable) events still have no home. The judgement call in the prompt - letting a receipt whose date has no open period fall forward to the nearest open date, as `banking.resolve_adjustment_date` does - was deliberately NOT taken; it changes which period real revenue lands in and is the user's call, not a refactor.

# Migration collision resolved (2026-08-11): the two vs_finance migrations numbered 0018 off the same parent are untangled. The allocation work renamed its file to `0019_allocation_effective_date.py` depending on `0018_alter_financeauditlog_action`, exactly as the Round 4 note asked - Round 4's choices-only migration was left untouched, so nothing depends on a file that was untracked at the time. `makemigrations --check` is clean and the finance chain is applied on the local dev DB through 0019.

# Voided documents stay in the customer's history (2026-08-11): `customer_account_movements` filtered `status=POSTED` while the void services set REVERSED, so voiding a receipt deleted it from the statement and the customer drawer and silently changed every running balance printed for a date BEFORE the void - the same "later events must not rewrite history" defect the as-of work had just closed everywhere else. A voided document now contributes two rows: the original on its own date, and an offsetting `Void` row dated when the reversal posted, so the balance is right at every point on the timeline. Reversal dates are resolved in one bulk query keyed on `JournalEntry.reverses` rather than by touching the reverse one-to-one per document, which would have been an N+1 down the length of a customer's history. The drawer loads POSTED+REVERSED for history while its open-invoice and open-debit-note panels re-filter to POSTED, because a voided invoice is history but is not owed; void rows report type VOID / status REVERSED instead of inheriting the document's settlement status. Aging, reconciliation and the statement aging block needed no change - they already key effectiveness off the journal through `_effective_on`. 6 regressions (5 in `VoidedDocumentHistoryTests` plus a drawer API case), 2 of which fail against the previous code, and one asserts the query count so the bulk lookup cannot silently regress. Finance 474/474 green.

# "As at" AR reports now describe that date (2026-08-11): aging, customer statements, reconciliation and dunning used `as_of` only as an aging clock while reading live balances, so a September settlement rewrote what a June report said and no historical figure could be reproduced twice. Settlement is now dated: `PaymentAllocation`/`CreditNoteAllocation`/`DebitNoteAllocation` carry an `effective_date` - the date of the journal that credited AR, so the sub-ledger and the GL agree at every point on the timeline - and the unique constraints are gone, because a row that accumulated two tranches could honestly carry neither date; rows are one immutable event each, matching how the void services already describe them. New `_ar_snapshot`/`_effective_on` rebuild AR from journal-dated evidence (mirroring `vs_procurement.reports._ap_snapshot`, which solved this first), covering all four settlement sources: cash, credit-note allocations, concessions and write-offs - the last read from the journal's AR credit because a request's `amount` may be blank meaning "the whole balance". Aging now ages open DEBIT notes alongside invoices instead of omitting them, so `reconcile_ar` stops adding them separately, and the control compares like with like by taking the cutoff on BOTH sides via a new `_account_gl_net_as_of` (journal lines, not per-period aggregates, since a cutoff falls mid-period). Dunning measures overdue and resolves notices as at the run date, with the daily path unchanged. Passing no cutoff preserves the current-state contract exactly. Migration 0018 backfills existing rows as max(crediting document, settled document) - a reconstruction, not a record, and exact wherever a pair settled in one run. 12 new regressions in `HistoricalARReportingTests`, 11 of which fail against the previous code; finance 468/468 green. No frontend change: nothing in the console consumes AR aging, the customer statement or a dated dunning run yet.

# Sub-ledger document voids keep AR and the GL together (2026-08-01): chose real document-level voids rather than refusal-only because users still need an auditable way to correct mis-keyed receipts and adjustments. Invoice, receipt, credit/debit-note, refund and concession void services now reverse every related journal and roll back authoritative settlement totals in one transaction; refund voids release the exact payment or credit-note lots they consumed, and completed installment plans reopen when settlement is removed. Later customer-credit allocation journals now have durable, entity-safe links with a legacy audit-log backfill. Raw journal reversal refuses any sub-ledger-owned journal with the document number and exact void endpoint, while existing document-level reversal services identify their owner explicitly. Reversal dates cannot precede the source and fall forward to today only when chronology and an open period allow it. Added five CRITICAL reverse permissions, tenant/RBAC/API regressions, double-void and closed-period coverage, and corrected AR reconciliation to compare invoice/debit-note receivables rather than customer-credit liabilities.

# Expense, payroll and tax settlements now respect their obligation dates (2026-08-01): expense reimbursements cannot predate the claim accrual, payroll disbursements cannot predate the payroll run, and tax remittances cannot predate the filing date. All three atomic services use the shared `chronology.ensure_on_or_after` guard and return `POSTING_BACKDATED` with a concrete boundary-date remedy. Added rejection and exact-boundary regressions for every service, with document state unchanged on rejection. The three frontend payment calendars enforce the same floors through `PostingDateField.notBefore`; the in-scope tax toolbar also wraps on phones. No matching workflow handlers or batch/list posting endpoints exist for these document types, so no duplicate preflight layer was needed. Finance: 413/413 tests green. Frontend: typecheck clean, 236/236 tests green, desktop/phone/tablet screens rendered without console errors; responsive overflow is zero after the tax toolbar fix.

# Dunning notices now actually dispatch (verified 2026-07-28): closed the 2026-06-30 gap. mark_notice_sent delivers a PENDING notice through vs_notifications (_dispatch_notice) before flipping it SENT - idempotent on an already-SENT notice, delivery failures logged there rather than swallowing the flip. run_daily_dunning (vs_finance/tasks.py) generates the day's overdue reminders then dispatches every PENDING notice, scheduled on beat at 06:00 daily (apps/apps/celery.py:44) and idempotent per (invoice, level) and per run date, so a missed window is safe. PENDING->SENT ownership resolved: the dispatcher owns the flip.

# Procurement reports documentation + full hardening (completed 2026-07-28): documented the live dashboard, AP aging/reconciliation and cash requirements, signed GR/IR balance/aging/GRN/PO-line drill-downs, spend, vendor performance, cycle time, API shapes, formulas, RBAC, tenant isolation, and test gaps in `docs/procurement/procurement_reports.md`. Fixed signed invoice-heavy GR/IR reconciliation, then completed every selected recommendation/judgment call: standard pagination on six primary reports and three invoice drawers while preserving whole totals; true journal/allocation-effective `as_of` AP and GR/IR snapshots with payment-reversal semantics; strict range/entity-category validation; explicit-first FIFO attribution for PO-only GR/IR clearing; gross cash buckets plus vendor-credit/net totals; full-settlement cycle timing with invalid-hop counters and monotonic end-to-end chains. Added historical, pagination, FIFO, credit-only, validation, instalment, and anomaly regressions. Independent procurement QA: 263/263 green; no migration drift; procurement permission seed remains idempotent at 61 keys. This completes all five procurement documentation slices.

# Procurement inventory documentation + hardening (2026-07-26): documented stock masters, immutable receipt/issue/adjustment movements, weighted-average valuation, reorder/valuation surfaces, exact GL carry/drop behavior, RBAC, tenant isolation, and test gaps in `docs/procurement/procurement_inventory.md`. Automatic fixes: receipt/issue/adjust lock and re-read authoritative stock rows (GRNs pre-lock multi-item batches in stable order); inventory-account changes are blocked while quantity/value remains; fractional-kobo paths use explicit half-up rounding with exact depletion; malformed master/movement inputs return stable 400s. User-selected follow-ups: GRN create/PATCH accepts active entity stock items by id/code and exposes their line fields; detail movement loading is SQL-bounded to the newest 50; receipt movements emit transactional `STOCK_RECEIVED` activity; reorder/valuation reports paginate SQL sources while keeping whole-entity totals. Added 17 regressions including a real same-item GRN race and invalid-input rollbacks. Independent procurement QA: 250/250 green; no migration drift; finance seed remains 33 accounts; procurement RBAC remains 61 keys. Only the optional issue/adjust cost-centre design decision remains.

# Procurement P2P selected follow-ups + PPV accounting (2026-07-26): completed the three user-selected fixes from `procurement_p2p_chain`. Cost-center ownership now propagates PO → GRN/invoice and into non-stock expense/PPV journal lines while inventory, GR/IR, tax, and AP controls stay unallocated; direct lines accept only active entity-scoped centers and source mismatches fail. Non-blank vendor invoice references now have a case-insensitive `(entity, vendor, reference)` database constraint, a collision-safe data migration, and friendly create/PATCH race errors. Receipt/PO-backed invoice price differences now clear GR/IR at linked-GRN-first/PO-fallback basis and post favorable/unfavorable deltas to seeded `5160 Purchase Price Variance` (IFRS Cost of Sales); direct/equal-price bills resolve no PPV. Corrected all GR/IR aging and GRN/PO-line drill-downs to use the same clearing basis. Migration 0015 applied locally; 5160 seeded idempotently into both local ledgers; full procurement QA 233/233 and finance chart seed 3/3 green; no migration drift. The only deferred P2P item is stock-item receipt input, owned by the inventory slice.

# Procurement P2P chain documentation + automatic safety fixes (2026-07-26): documented PO → GRN → vendor invoice → vendor payment in `docs/procurement/procurement_p2p_chain.md`, including endpoint bodies, independent workflow/ledger/settlement states, kobo formulas, exact GR/IR/AP/bank/WHT journals, carry/drop behavior, permissions, and test gaps. Fixed five unambiguous defects found by the trace: direct-GRN bills now clear GR/IR instead of double-expensing; GRN posting locks/re-reads receipts and PO lines, aggregates live quantities, and prevents duplicate posts/over-receipt; receipt/bill quantities reject non-finite, oversized, and over-precision values atomically; PO-backed receiving requires an APPROVED PO at API and service boundaries; `allow_variance` is a strict JSON boolean and requires the new CRITICAL `procurement.vendor_invoice.override_variance` key with canonical action seed and audit evidence. Added real PostgreSQL race, ledger, validation, approval, permission, seed, and caller-compatibility regressions. Full procurement QA: 220 tests green; no migration drift; permission seed created the new key and granted both platform roles. Remaining P2P decisions: cost-center carry-through, database invoice-reference uniqueness, stock-item receipt API, and purchase-price-variance accounting.

# Procurement sourcing slice + hardening (2026-07-22): documented requisitions, RFQs, invitations, quotations, workflow approval, pricing, and award-to-draft-PO conversion in `docs/procurement/procurement_sourcing.md`. Money fixes: requisition POST/PATCH uses strict positive/finite/bounded quantities with atomic rollback; quotation award carries source/header cost-center ownership onto PO lines with select-related lineage so sourced commitments appear in budget availability. Follow-up decisions implemented: RFQ source lines must match a named header requisition; all sourcing transitions share deadlock-safe RFQ→quotation→vendor locking with authoritative rechecks; multiple quotations per vendor remain allowed, with newest (`created_at DESC, id DESC`) canonical and listed newest-first. Response deadlines remain buyer-closed and quotation expiry remains display-derived by design. Added six regressions across malformed replacement, award→budget visibility, lineage/rollback, canonical response order, and two real PostgreSQL races. Full procurement QA: 209 tests green; no migration drift; permission seed 0 new/60 total; demo seed idempotent. No open sourcing decision remains.

# Procurement master-data slice hardening (2026-07-22): documented categories, vendors, catalog items, contracts, and milestones in `docs/procurement/procurement_master_data.md`, then fixed every actionable item found by the trace. Contract `auto_renew` now accepts only real JSON booleans; renewals `within_days` validates a whole 0..3650-day horizon; termination and milestone completion lock/re-read rows atomically so renewal/completion races serialize and audit once; vendor KYC/risk/hold changes now require both `procurement.vendor.update` and the new restricted `procurement.vendor.manage` permission (ordinary edits remain update-only). Added 13 regressions across validation, stale/idempotent actions, real PostgreSQL thread races, compliance denial/success, and idempotent permission seeding. Full procurement QA: 203 tests green; no migration drift; permission seed idempotent. Only justified-by-design notes remain in §8.

# Tenant/date document numbering refactor (2026-07-22): introduced one shared, concurrency-safe `TenantDocumentSequence` keyed by `(tenant, document_code, local date)` and formatted every new reference as `<CODE>-<tenant_id><YYMMDD><daily_sequence>` with an unpadded counter. Finance and Procurement retain their document codes (`IV`, `PY`, `PO`, `GN`, `CT`, etc.) while dropping entity/branch/fiscal-year tokens; Payments retains `CXP`; support tickets replace the slug/CX form with `TK`. Multiple ledger entities owned by one tenant now share each code's daily series. Existing stored identifiers and legacy Finance/Ticket counter metadata are preserved; migration `vs_tenants.0003_tenantdocumentsequence` is additive only. Finance/Payments docs updated, Procurement covered through its inherited numbering and exact GRN/contract tests. QA: 613 affected-app tests green in one sequential PostgreSQL run; system check clean; no migration drift; diff check clean.

# Session 2026-07-20 - CI green on both repos, 4 features, organogram fixes (all pushed to main). CI FIXES: backend was red at the schema-gen step (ImproperlyConfigured: ComplianceRule.school→tenant left in vs_audit serializers/views - commit 11bbc20), which hid two more layers surfaced once schema passed: (a) vs_payments payout→post_vendor_payment blocked by the new procurement approval/KYC/on-hold gates - added a system_originated bypass for already-disbursed gateway payouts (9ab35b1), (b) stale FLS test vs the more-secure vendor serializer (folded into f8aef8b area). console-fe was red on a userId number→string typecheck (already fixed in f02d283) + 26 eslint-plugin-react-hooks v7 errors swept (ec961b2). FEATURES: (1) Remove OPay entirely - backend 979d33a (PaymentProvider.OPAY + adapter + settings + tests gone, migration 0003 alters provider choices), frontend 4d2de3a (provider maps + VA dropdown). (2) Password policy - one canonical rule (12 + upper/lower/digit/special) enforced by PasswordComplexityValidator + advertised at GET /auth/password/policy/ (f8aef8b), live requirements checklist on all four screens sign-up/reset/activate/change (3e6e609). (3) Ticket renumber - <SLUG>-CX<YYMMDD><n>, per-tenant per-day, un-padded, new-format-only (989f269); owning-tenant row on ticket detail (7fc65cc). (4) Draft CX hires + CSV bulk upload - User.Status.DRAFT, save_as_draft, /users/<id>/submit/, /users/bulk-template/ + /users/bulk-upload/ with per-row validation (39909a1); "Save as draft" + BulkUploadModal + submit-draft action (70613bd). ORGANOGRAM: build_tree surfaces orphaned subtrees under an inactive parent (caefbea); killed the N+1 that hung the Manage page - prefetch current holders + annotate child count (4ec1d07); made seed_organogram idempotent (was duplicate-keying on the OrgNode tier-prefixed code) and SEEDED local cx_db = 7 nodes / 7 positions / 41 staff, VP-TECH root (5f6e74d). Both CI pipelines green (backend 6m29s, console-fe 2m31s).

# vs_payments webhooks/providers slice - DONE + hardening (2026-07-12, Opus subagent under Fable QA): third and FINAL vs_payments module-docs slice (docs/payments/payment_webhooks_providers.md - the public signature-verified webhook receiver, the verify→dedupe→store→dispatch→RE-VERIFY pipeline, and the Paystack/OPay/Fake adapters behind the neutral Provider interface + registry + stdlib HTTP transport + typed exceptions), then three fixes. (1, async) Webhook processing is now off-request: ingest_webhook stores-and-acks fast (RECEIVED 200) and transaction.on_commit → new Celery task vs_payments.process_webhook_event (tasks.py, auto-discovered); the outbound PSP re-verify + booking run in new process_stored_event on a worker - idempotent (no-op if gone/PROCESSED), failure marks FAILED and is swallowed (PSP re-delivers; confirm_* idempotent). Under CELERY_TASK_ALWAYS_EAGER (local/CI/pre-worker staging) it still runs inline. _dispatch unchanged. (3, audit-once) WEBHOOK_RECEIVED PaymentEvent written only when the event is first created, so a provider retry of a not-yet-processed event no longer duplicates the audit row (self-heal preserved: IGNORED/FAILED events still reprocess on re-delivery, DuplicateWebhookError only short-circuits PROCESSED). (2, tests) Added fixture-based unit tests for the REAL adapters (were only exercised via FakeProvider): PaystackAdapterTests (6) + OPayAdapterTests (7) patch request_json AT POINT OF USE (providers.paystack/​opay.request_json - adapters bind it via `from .http import`) covering checkout/verify/transfer mapping, non-ok→ProviderError, OPay VA unsupported, HMAC signature pos/neg, parse_webhook direction routing; + WebhookProviderResolutionTests (unknown provider→ProviderNotConfiguredError) + WebhookTests.test_ingest_is_idempotent_and_audits_once. Async test updates: webhook-processing tests wrapped in self.captureOnCommitCallbacks(execute=True) + refresh_from_db. 15 new tests; full vs_payments suite green: 70 (was 55); no migration drift; seeds idempotent (13 keys). THIS COMPLETES vs_payments module docs (3 slices: collections, settlement, webhooks_providers) - next module per playbook is vs_procurement. Remaining OPEN payments items (documented, low-pri): transport-error paths in http.request_json untested; IGNORED self-heal-on-retry not pinned; plus the settlement §8.1 go-live todo (seed payout_batch approval templates).

# vs_payments settlement hardening - slice 2 gotchas 2,3,4,5,6,7 (2026-07-12, Opus subagents under Fable QA): second vs_payments module-docs slice (docs/payments/payment_settlement.md - payouts, batches + the new vs_workflow approval gate, settlement reconciliation, movements, transactions log), then five fixes. (2) Webhook events now carry the matched collection/payout's entity - ingest_webhook resolves the record once (_find_record) and attributes the WEBHOOK_RECEIVED PaymentEvent to its entity, so gateway webhooks appear in that entity's transactions log; bad-signature WEBHOOK_REJECTED stays entity=NULL (payload untrusted). (3) Payouts adopt the provider-reported settled amount like collections: TransferResult gains an `amount` field (populated by Paystack `data.amount`, OPay nested `amount.total` best-effort, Fake `forced_amount`); confirm_payout books `settled = amount or payout.amount`, stashing `instructed_amount` in metadata when it differs (0 never overrides). (5) reconciliation fee_amount clamped `max(0, |gross|−|settled|)` so over-settlement never reads negative. (6) Payout-batches "queued" KPI sums in-flight child instructions (PENDING/PROCESSING, batch-linked) instead of batch total_amount, so a FAILED child no longer inflates money-in-flight. (7) Movements feed drops internal `linked_id` (payment_id/vendor_payment_id); narration intentionally kept, beneficiary PII still FLS-masked. (4, follow-up) reconciliation amount-fallback now matches the DATE-NEAREST bank line among equal amounts (new _closest: prefer bank date on/after confirmation, then smallest day-distance, then lowest id) instead of first-unconsumed-wins - deterministic/order-independent; Pass 1 reference matching untouched. 7 new tests total; full vs_payments suite green: 55 (was 48); no migration drift; seeds idempotent (13 keys). OPEN: §8.1 maker-checker is opt-in per template (added Undone go-live task to seed payout_batch templates). NOTE: user had unrelated concurrent work (vs_config/serializers.py, vs_health/*, AGENTS.md) in the tree - not staged; test DB was intermittently locked by the user's parallel test run, so QA reruns waited for it to clear.

# Notification system overhaul - global, recipient-centric (2026-07-07, commit feb08dc; two Opus subagents under Fable QA): vs_notifications no longer assumes school tenancy. Notification.school is now an optional filter/history scope (send_notification school=None default); SchoolNotificationSetting → NotificationSetting with nullable school (NULL = platform-wide default row; conditional unique constraints; migration 0004 reversible, seeds platform rows). Channel resolution layers school row → platform row → event_type.default_enabled via resolve_channels/_bulk (single query; the old fail-open-with-warning hack removed). NEW NotificationEventType.is_transactional (password resets/invites always dispatch; is_active kill switch still wins). HTML email: NotificationTemplate.html_body + rendered Notification.html_body, multipart via core.mail.send_email (html_message now optional), per-message From via metadata.from_name. NEW signals notification_sent/notification_failed on terminal delivery + internal-only Notification.metadata (never serialized). Settings API rebuilt: GET = effective matrix with provenance, PATCH upserts by (event_type_key, channel); CX staff manage platform or ?school= scope; school users 404 on foreign ?school=. Security: feed retrieve now recipient-scoped 404 (removed existence-leaking 403 + vision-staff bypass); history hard-scopes school admins; views rewritten against the REAL core.response signatures (old kwargs raised TypeError at runtime - latent bug). Delivery task: select_for_update now inside atomic (Postgres fix), SMTP send outside the row lock, eager-mode first failure is final (no retry raise into requests). Bypass paths consolidated onto the engine: vs_user invitation + password-reset emails (file templates deleted, ported to seeded DB templates incl. conditional subjects; UserInvitation email tracking via signal receivers in vs_user/receivers.py correlated by metadata.activation_key), vs_todo send_completion_review_request, core TrackedTask._notify_owner (both dropped runtime event get_or_create; registry seeds task.completed/task.failed/todo.task_completed/user.password_reset). Channels remain in-app + email only. Tests: vs_notifications 3→32; suites green on Postgres: vs_notifications+vs_user+vs_todo+core+vs_admin_console+vs_workflow = 182, vs_finance = 305; no migration drift. Deploy seeding handled: build.sh now runs seed_notification_event_types + seed_notification_templates + seed_notification_settings after seed_all_permissions (all idempotent; settings command seeds PLATFORM school=NULL rows, per-school overrides via --school/--all); seeds also run + verified on local DB 2026-07-07. FRONTEND FOLLOW-UP PENDING (see undone).

# FAL .docx exports regenerated (2026-07-05, Sonnet subagent under Fable QA): both FAL_Interface_Spec.docx copies (FAL/ root + XVS_Finance_Abstraction_Layer_Spec_v1/) rebuilt from the canonical v1.1 markdown with §16 decisions applied - title page, TOC, real Word tables, code styling; identical MD5; OOXML-validated; independently grep-verified ("Decisions (resolved 2026-07-04)" present, zero "open question" remnants). Circulation-ready.

# Invoice-detail full settlement + payment-plan waiver fix (2026-07-06): two reported bugs. BUG A - the invoice detail drawer (GET /finance/invoices/<id>/, InvoiceDetailView) only surfaced cash: `summary.paid`/`payments`/`gl_postings` read amount_paid + the original AR journal only, so credit notes, concessions and write-offs that reduced the balance were invisible (paid=0, ledger null even though balance dropped). Fix (backend, additive - kept `payments`/`gl_postings` for existing consumers): InvoiceSerializer now exposes amount_credited + settled_amount; view adds summary.credited/settled, a unified `settlements` list (PAYMENT/CREDIT_NOTE/CONCESSION/WRITE_OFF, each tagged `type`), `gl_journals` (invoice + every settlement journal grouped per source doc), and credit-note/concession/write-off events in `activity`. Write-offs have no allocation row + no invoice FK on their journal, so they're pulled from the audit log (INVOICE_WRITTEN_OFF, metadata.journal_id/amount). BUG B - a pre-plan waiver was double-counted: plan total_amount defaults to invoice.balance_due (already net of the waiver) but refresh_plan_progress distributed invoice.settled_amount (incl. the waiver credit) across installments, so installment #1 showed part-settled the moment the plan activated (₦967,500 → ₦645,000 due). Fix: PaymentPlan gains baseline_settled (migration 0029), snapshotted at activation = invoice.settled_amount; refresh_plan_progress counts only settlement BEYOND the baseline. New tests: PaymentPlanTests.test_pre_plan_waiver_does_not_pre_settle_installments + InvoiceDetailEndpointTests.test_detail_surfaces_credit_note_concession_and_write_off_settlements. Full vs_finance suite green: 271 tests. FRONTEND FOLLOW-UP PENDING (see undone) - the drawer must consume the new fields.

# Receipts settle DEBIT notes (2026-07-05): fixed a reported bug - a debit note raised with no invoice (DN 20k) plus a receipt (40k) left the DN unsettled forever and parked the whole 40k as customer credit (2140), overstating credit by the DN amount, with no way to allocate the receipt to the DN. Root cause: a DEBIT note debits AR like an invoice, but the receipt allocation engine only ever settled Invoice rows. Fix makes DEBIT notes first-class AR open items. Model: CreditNote gains amount_paid + settlement_status (+ balance_due / refresh_settlement_status, mirroring Invoice) and a new DebitNoteAllocation(payment, note, amount) link table (migration 0028, additive/reversible). Engine (receivables.py): _build_invoice_plan gains include_debit_notes (payment paths pass True; credit-note sub-ledger stays invoice-only) and yields open invoices + open DEBIT notes ordered together (oldest/largest); _apply_payment_subledger branches on target type (Invoice→PaymentAllocation / bump amount_paid; DEBIT note→DebitNoteAllocation / bump amount_paid + settlement_status). No new GL: the DN's AR debit and the receipt's Cr AR for the applied portion net off; only true excess hits 2140. customer_credit_balance now nets unsettled DN balances (floored at 0). API (views_ar.py): explicit allocations accept {debit_note, amount}; payment-detail lists open_debit_notes; _customer_ledger counts open DN balances in outstanding; customer statement/transactions include debit notes (running balance was wrong before). _resolve_debit_note is POSTED+DEBIT+entity-scoped. 4 new tests (standalone settle = the reported scenario, explicit DN allocation, stored-credit drains onto DN, mixed invoice+DN oldest-first); existing "DEBIT note cannot allocate/ → 400" invariant preserved. Full vs_finance suite green: 270 tests; migration applies clean; check clean. Docs updated (finance_ar_adjustments §2/§4/§5/§6/§8/§10/§11, finance_invoicing_ar §1/§2/§3/§6/§8). Remediation for already-posted data: call POST /finance/payments/<id>/allocate/ {auto_allocate:true} on the existing receipt to drain its stored credit onto the open DN.

# vs_payments collections hardening - slice 1 gotchas 1–4,6,7 (2026-07-04, Opus subagent under Fable QA): first vs_payments module-docs slice (docs/payments/payment_collections.md), then six fixes. (1) Confirmed collections now book the provider's SETTLED amount, not the requested one - confirm_collection adopts `settled = amount or intent.amount`, stashes requested_amount on metadata when they differ; webhook threads parsed.amount through (a 0 report never overrides). (2) Standalone (no-invoice) collections park cash as customer credit (2140) via post_payment(auto_allocate=False) instead of silently auto-settling the customer's oldest open invoices (confirmed use case). (3) initiate_collection rejects a customer-less collection at initiation (ValidationError→400) before any provider call - no more un-bookable orphan intents. (4) "One ACTIVE virtual account per customer per provider" now enforced: partial UniqueConstraint (entity,provider,customer where ACTIVE & customer not null) + friendly pre-flight guard (migration 0003). (6) VA list uses the standard XVSPagination {pagination,data} envelope (page size 20→25, real next/previous) with kpis injected - was hand-rolled. (7) A deposit on an INACTIVE VA is held (PaymentStateError → webhook FAILED/replayable) instead of auto-posting to a deactivated NUBAN. 6 new tests; full vs_payments suite green: 38 tests (was 32); no migration drift; seeds idempotent. Left OPEN (documented §8): REFUNDED dead state (by design, refunds later), account_number search FLS oracle (low sev, settlement/FLS review). Migration 0003 also bundles a pre-existing choices-only AlterField on paymentevent.action (enum drift from 0002; no DB effect, reversible).

# Gotcha-sweep follow-up fixes F1-F5 + J2-J4 (2026-07-03, Opus subagent under Fable QA): (F1) GET /finance/entities/ now tenancy-scoped in the queryset (CX staff all; school users only their own; school-less none) - defence-in-depth on top of the platform-only key grant. (F2) refunds pagination was ALREADY covered by the earlier ops pagination sweep - stale doc gotcha, flipped. (F3) asset disposal blocked while depreciation due ≤ disposal_date is unposted (post first; future rows still orphan on early disposal); pre-existing disposal test adjusted to comply. (F4) customer create accepts optional opening_date to backdate the opening-balance invoice. (F5) tax filing due_date defaults to day filing_day of the month after period_end (clamped, Dec rolls year; explicit date wins). (J2) roster/structures split to new finance.salary.{view,create,update,delete} resource (all SENSITIVE); FLS pay-figure key stays payrollrun.view_sensitive; OPS: re-grant salary.* to roles that managed rosters via payrollrun.create. (J3) account-create parent accepts code or pk. (J4) DELETE /budgets/<pk>/ for drafts (finance.budget.delete key, BUDGET_DELETED audit action, migration 0027). 13 new tests; suite 282 green (independent run); seeds idempotent. Justified-not-fixed (documented in slice docs §8): cost-center upsert semantics (FE depends), legacy pre-token file names, plus all intentional-design items.

# Seven finance hardening fixes (2026-07-03, implemented by an Opus subagent under Fable QA): (1) periods/<id>/reopen/ + /lock/ endpoints wired (new CRITICAL keys finance.period.reopen/.lock + canonical reopen/lock actions seeded) - LOCKED now reachable, mis-closed months recoverable via API. (2) tax-filings/<pk>/unfile/ - FILED→DRAFT, reverses the netting/penalty journal, refused once any remittance recorded; new TAX_FILING_UNFILED audit action (migration 0024). (3) Overlapping tax-filing periods rejected at prepare (also blocks re-drafting an already-FILED window) - closes a double-remittance hole. (4) run-depreciation now posts one compound journal PER fiscal period (backlog no longer collapses into one month); response keeps journal_id, adds journal_ids + period_count; CLOSED period in range raises cleanly; QA follow-up added a typed DepreciationError for dates with no fiscal period. (5) Depreciation is ACTIVE-only (DRAFT assets refused). (6) FinanceAuditLog immutable at the DB level - BEFORE UPDATE/DELETE triggers, PG + MariaDB branches, reversible (migration 0025). (7) Dead BudgetStatus.LOCKED removed (migration 0026; is_locked == APPROVED; reports.py lookup updated). 16 new tests; full vs_finance+core suite green: 269 tests, 0 failures; no migration drift; seeds idempotent. Docs synced (reports_statements, tax_remittance, fixed_assets, audit_trail, budgets).

# Payment plans auto-sync on settlement (2026-06-30): closed the pull-based-progress gap documented in finance_payment_plans §8. New installments.refresh_plans_for_invoice(invoice) refreshes every ACTIVE/COMPLETED plan linked to an invoice; called from the per-invoice settlement points - _apply_payment_subledger (receivables.py, receipts), _apply_creditnote_subledger (credit_notes.py, credit-note allocation) and _write_off_invoice_atomic (credit_notes.py). Concession posting already refreshed directly. So a receipt now advances the plan with no manual refresh/ call (that endpoint remains for standalone plans + settled_amount overrides). No GL effect. Test PaymentPlanTests.test_receipt_auto_refreshes_linked_plan; full vs_finance suite green (208). Docs §1/§4/§8/§11 updated.

# AR-adjustments cleanups (2026-06-30): two issues found while documenting finance_ar_adjustments, both fixed. (1) Credit/debit-note posting now carries the line's cost_center to the GL - _post_credit_note_atomic groups revenue by (account, cost_center) instead of account-only (credit_notes.py:125), matching the invoice/expense/payroll/petty-cash propagation; tax stays aggregated. Regression test CreditNoteTests.test_credit_note_revenue_line_carries_cost_centre_to_gl. (2) Fixed stale Refund model docstring (models/adjustments.py:183) that claimed "Dr AR control, Cr bank" - actual posting is Dr customer-credit (2140), Cr bank. Full vs_finance suite green: 203 tests, 0 failures. Docs updated (finance_ar_adjustments §1/§6/§8/§11, finance_cost_centers §6 table).

# AR invoicing flagged fixes (2026-06-30): three §8 gotchas from docs/finance/finance_invoicing_ar.md fixed. (1) Customer + receipt lists now paginate via XVSPagination (no more silent [:500] truncation) - PaymentList's allocation_status filter moved into the DB query so page counts are right (views_ar.py). (2) Customer.opening_balance now posts an opening invoice (source=OPENING, Dr 1200 AR / Cr 4100 Operating Revenue) via post_opening_balance (receivables.py:188) on customer create, atomic so a posting failure rolls the create back; surfaces in invoice-derived outstanding + GL, off P&L. Added InvoiceSource.OPENING (migration vs_finance/0018_alter_invoice_source). (3) Auto-allocation strategy: _build_invoice_plan takes strategy oldest|largest; post_payment/allocate_payment thread it; receipt/allocate endpoints accept allocation_strategy (unknown → 400). Tests in FinanceAPITests (opening balance, largest-first, bad strategy). Full vs_finance suite green: 187 tests, 0 failures; no migration drift. NOTE left intentional: post=True default on invoice create, pay/ requires POSTED, AR defaults to 1200.

# Cost centers now reach the General Ledger (2026-06-29): the gap (sub-ledger postings aggregated GL lines by account only and dropped cost_center) is FIXED. P&L lines now group by (account, cost_center) and pass it through - post_invoice (receivables.py:133), post_expense_claim (expenses.py:89), petty-cash voucher (petty_cash.py:170), and post_payroll splits gross salary by cost centre (payroll.py:198). Balance-sheet control/tax/payable lines stay aggregated (intentional). direct-entries also now accept an optional per-line cost_center (code/id), resolved in the view (views.py:879) and written by post_direct_entry (posting.py:334); unknown code → 400. Regression tests: vs_finance CostCenterPropagationTests (invoice + expense) + PayrollTests.test_accrual_splits_gross_salary_by_cost_centre + FinanceAPITests.test_direct_entry_carries_cost_centre_to_gl / _rejects_unknown_cost_centre. Also fixed a stale test import (InvoiceListView → InvoiceListCreateView) that was erroring 4 endpoint tests. Full vs_finance suite green: 181 tests, 0 failures (--settings=apps.settings.local). Docs: docs/finance/finance_cost_centers.md §6/§8/§11 + finance_journals_posting.md updated. NOTE: journals posted before this change have no cost_center on their GL lines (historical gap).

# vs_health observability (2026-06-14): NEW app vs_health mounted /v1/health/, the backend for the Health screens (5 screens + Tenant Health). Self-instrumentation, not an external metrics pipeline: RequestMetricsMiddleware (after tenant context) folds per-request route/method/status/latency/school into 1-minute RequestMetric rollups with a fixed latency histogram (p50/p95/p99 estimated by merging histograms); in-process buffer flushed every ~30s with select_for_update + merge so concurrent workers share rows. Models (models/ package): MonitoredService, Deployment, SLO, RequestMetric, UptimeCheck/Result/DailyRollup, QueueSnapshot, Incident/IncidentEvent, AlertRule/Alert. Probes (http/tcp/redis/postgres/ssl) + 5 Celery-beat tasks (run_uptime_checks, capture_queue_snapshot, evaluate_alert_rules w/ auto-open+resolve incidents, rollup_uptime_daily, prune_health_metrics). Task table reuses core.BackgroundJob (no new task tracking). services.py owns all aggregation. API: overview, services, uptime/monitors, api-endpoints(+detail), queues, tasks, incidents(+events,reliability), alerts, alert-rules, tenants, deployments, slos - reads gated platform.health.view, writes platform.health.manage (xvs_super_admin bypasses); metric tables are global/unscoped (observability spans all tenants). seed_health command registers the two RBAC perms + 12 services + 7 checks + 5 rules + 4 SLOs + sample incident + synthetic history. 11 tests pass (--settings=apps.settings.local; SQLite fails on the pre-existing core 0003 CASCADE migration), system check clean, no migration drift. Wired INSTALLED_APPS, MIDDLEWARE, apps/urls.py, apps/celery.py beat_schedule.

# Permission seed completeness (2026-06-12): the platform permission keys (platform.organogram.*, platform.staff_profile.*, staff_payroll, team, schools, branches, audit, dashboard, permissions, roles) were defined ONLY inside create_superuser and were NOT run by seed_all_permissions - so organogram (and friends) were missing from the canonical seed. Fixed by extracting a single source of truth: NEW core command seed_platform_permissions (creates the platform module/resources/permissions + grants all to xvs_super_admin and all-except-platform.roles.transfer to xvs_platform_admin; idempotent, graceful if roles/actions absent). create_superuser now creates the two platform roles then delegates to it (removed the duplicated inline PLATFORM_RESOURCES spec + SUPERUSER_PERMISSION_KEYS + grant loops, eliminating the drift that caused the gap). Also NEW vs_todo command seed_todo_permissions (todo.task.view/manage/assign → both platform roles). Both registered in seed_all_permissions SEED_STEPS (now 9 steps; platform at #3, todo at #9). 4 new tests in vs_todo (organogram captured, transfer-boundary grants, todo keys granted, full seed_all runs clean) → 19 vs_todo tests pass (--settings=apps.settings.local), check clean.

# B7 COMPLETE (2026-06-12): Render worker live - existing Oregon Key Value (Valkey 8.1.4,
# volatile-ttl) reused as broker; Background Worker runs celery -A apps worker -B. First beat
# tick verified in production logs: dispatch-pending-import-notifications found and sent 2 REAL
# import notifications that had been stuck in staging since before any scheduler existed -
# the exact B7 failure mode, observed clearing itself. Emergency lever: CELERY_EAGER=true on
# the backend reverts to in-process execution without a redeploy.

# B17 (2026-06-12): PostgreSQL standardised across local/CI/staging. Local dev now runs PG 16
# (brew postgresql@16, db cx_db; DB_ENGINE=mysql falls back to the untouched MariaDB). New
# apps/settings/ci.py + .github/workflows/ci.yml (postgres:16 service; check, migration-drift,
# fresh-migrate, full suite on every push/PR to main/staging). PG portability bugs found & fixed:
# (1) B23 migration - Django's `_like` varchar_pattern_ops indexes must be dropped before the
# BIGINT FK conversion; (2) vs_workflow locking - select_for_update + select_related over nullable
# FKs = FOR UPDATE on an outer join, rejected by PG → lock only the target row; (3) role-name
# case-insensitive uniqueness moved to model save() (MySQL ci collation gave it free; PG functional
# index isn't portable to MariaDB). Full suite (520 tests incl. vs_todo) green on PG.

# B7 code side (2026-06-12): beat schedule in apps/celery.py (dispatch notifications */5, retry
# failed */15, mark stuck */30, cleanup batches daily 02:00 - all idempotent); staging eager mode
# env-gated (CELERY_EAGER, default true = safe without worker); render.yaml blueprint (web + worker
# with embedded beat + free Key Value). Worker creation + env flip = user dashboard steps above.

# vs_todo (2026-06-11): NEW app "ToDo - Org Accountability" (mounted /v1/todo/), built from the offline HTML design (~/Downloads/ToDo Accountability (offline).html). CX-staff intranet tool: every Task belongs to one person, a manager rolls up their whole area (self + all reports), and assignment only flows DOWN the organogram. The reporting tree is NOT stored - it is derived live from the existing vs_user organogram (Position.reports_to / PositionAssignment) via services/hierarchy.py (descendant_users / direct_report_users / area_user_ids / is_manager / can_assign / chain_to), mirroring the design's tree helpers. Model Task (assignee, assigned_by[+name snapshot], title/description/metric/target, deadline, priority, department snapshot, is_done + completed_at); status (Completed/In Progress/Overdue) is a derived property, never stored. Services: tasks.py (create_task enforces assign-down + dept snapshot, set_done, can_view/can_modify), stats.py (stats_for + own/area task querysets), dashboards.py (node_dashboard + efficient single-fetch org_rollup tree). REST: TaskViewSet (CRUD + /toggle/, area-scoped get_queryset, ?assignee/?status filters) + dashboards GET /v1/todo/dashboard/{mine,team(?focus=),org}/ + /assignable/. Gated by IsAuthenticatedAndActive & IsVisionStaff; envelope via core mixins/success_response. Migration vs_todo 0001. 15 tests pass (--settings=apps.settings.local), check clean. NOTE: sqlite test path stays broken by the MySQL-only raw-SQL migrations (see B17) - unrelated to vs_todo.

# B23 (2026-06-11): School slug-PK → surrogate BigAuto id, slug stays unique. ONE migration
# (vs_schools 0003) flips state + remaps the DB: drops/rediscovers FK + CHECK constraints dynamically,
# adds+populates id, rewrites all 21 school FKs slug→id, retypes them BIGINT, restores constraints.
# Vendor branches for MariaDB (local) + PostgreSQL (staging); fresh-install replay validated by the test
# runner. JWT now carries numeric school_id + NEW school_slug claim; middleware/serializers accept id OR
# slug (SchoolField renders slug so the wire format is unchanged). Slug-based URLs (school__slug) untouched.
# Dev DB backup taken pre-flip: /tmp/cx_db_backup_before_b23.sql. STAGING: back up Postgres before deploying this.

# B9 follow-up (2026-06-11): media = ONLY import CSVs + images (user decision), so uploads are now
# DATABASE-backed: core.StoredFile + core.storage.DatabaseStorage (extension allowlist csv/xlsx/xls/png/
# jpg/jpeg/gif/webp - svg deliberately excluded, MEDIA_DB_MAX_BYTES=25MB ceiling, path-traversal guard),
# served with auth by core.views.MediaView at /media/<name> in every env. Survives ephemeral-disk
# redeploys, ships with DB backups; outgrowing it = point STORAGES["default"] at S3 and copy rows out.
# Side-fix: staging's STATICFILES_STORAGE was removed in Django 5.1 and silently ignored - WhiteNoise
# is now wired via STORAGES correctly.

# B2 follow-up (2026-06-11): TenantAwareManager extended to LoginSession/AuthAttempt/AuthEventLog,
# Notification/SchoolNotificationSetting, WorkflowTemplate(include_global)/WorkflowInstance(composed with
# its domain queryset)/ApprovalDelegation, ComplianceRule(include_global), ConfigurationChangeLog
# (tenant_field="institution", include_global). Manager grew include_global + tenant_field options.
# vs_user.User deliberately EXCLUDED (auth machinery resolves users before any tenant context exists).

# B21 durable audit (2026-06-11): vs_rbac.RBACAuditLog (append-only, immutable) + audit.record_rbac_audit -
# durable row written transactionally with the action, central vs_audit kept as guarded best-effort mirror;
# all 35 emit sites in signals.py/services.py converted via import swap; vs_config already had its own
# append-only ConfigurationChangeLog so needed nothing. Migration 0006. Tests in vs_rbac/tests/test_audit.py.

# B25 module splits (2026-06-11): vs_finance/models.py (2.4k) → models/{core,gl,ar,adjustments,dunning,ops};
# vs_finance/views_ops.py (1.6k) → views_ops/{base,masterdata,banking,expenses,pettycash,tax,payroll,budgets,assets,audit};
# vs_procurement/views.py (1.8k) → views/{base,vendors,contracts,catalog,requisitions,orders,receiving,vendor_payments,reports,stock};
# vs_user/views.py (1.6k) → views/{me,auth,passwords,accounts,security,organogram};
# vs_rbac/serializers.py (1.5k) → serializers/{registry,school,platform}. All via package __init__ star
# re-exports - import paths unchanged, no migration drift, full suite green.

# vs_rbac test-suite repair COMPLETE (2026-06-11): all 214 tests green. Along the way fixed two REAL bugs the
# rotted tests were hiding: (1) school role detail/update/delete routes declared <int:id> but
# SchoolRoleTemplate.id is a slug - endpoints could never match; now <slug:id>. (2)
# SchoolRoleChangeRequestDecisionView.post() didn't accept the school_slug URL kwarg - every decide call 500'd;
# now accepts it and scopes the lookup by school. Also restored lost invariants with real constraints:
# uq_school_role_name (school,name), uq_platform_role_name (name) + dedupe data migration 0005, and app-level
# one-ACTIVE-assignment guards on both assignment models (conditional unique isn't portable to MariaDB).

# 0u. Tiered org structure DIVISION → DEPARTMENT → TEAM + Department→OrgNode rename (user decision: enforce hierarchy; model renamed to OrgNode). (A) models.py: Department renamed to OrgNode (generic tiered node); +kind TextChoices (DIVISION/DEPARTMENT/TEAM, default DEPARTMENT); clean() now enforces strict tiering via _REQUIRED_PARENT_KIND map (DIVISION must be top-level/no parent; DEPARTMENT must sit under a DIVISION; TEAM must sit under a DEPARTMENT) on top of the existing self-parent + cycle guards; +nearest_of_kind(kind) helper (walks self→ancestors); head_position related_name heads_department→heads_node; db_table vs_users_department→vs_users_org_node; +index on kind. Position.department FK renamed to Position.org_node (related_name 'positions'); a seat may hang off ANY tier. PlatformStaffProfile: .department is now a DERIVED property = org_node.nearest_of_kind(DEPARTMENT) (a Team seat resolves to its parent Department; sitting directly on a Division → None); +new .org_node property = the exact node the seat is in. (B) services/organogram.py: import OrgNode; build_tree/vacancies/primary_position_for select_related 'org_node'; tree node dict key department→org_node; resolve_department_head walks org_node.parent up Team→Department→Division to the nearest filled head seat. (C) serializers.py: DepartmentInlineSerializer→OrgNodeInlineSerializer (+kind); DepartmentSerializer→OrgNodeSerializer (+kind); OrganogramNodeSerializer→OrgTreeNodeSerializer (department→org_node); PositionSerializer department/department_id→org_node/org_node_id; PositionInlineSerializer department→org_node; profile serializers expose position + org_node + derived department. (D) views.py + urls.py: DepartmentViewSet→OrgNodeViewSet (+?kind= filter); route /organogram/departments/→/organogram/nodes/ (basename org-nodes); Position viewset/tree select_related + ?org_node= filter; profile queryset ?org_node= (PK or code) via position__org_node, select_related position__org_node. (E) Migrations (data-preserving, MariaDB-safe): 0009 hand-written RenameModel + AlterModelTable + RenameField(position.department→org_node) + AddField(kind); 0010 cosmetic (verbose_name, head_position related_name, kind index, index renames) - the Position index drop was converted to RenameIndex to dodge MariaDB's FK auto-index management (dropping an FK-covering index calls _create_missing_fk_index which looked up the now-gone 'department' field and crashed); 0011 RenameIndex to a stable explicit name pos_orgnode_active_idx; 0012 SeparateDatabaseAndState state-only fix (RenameIndex updates the name but NOT the index field list in migration state, so state still said fields=['department'] → autodetector kept emitting a failing drop; reconciled state to fields=['org_node'] with zero DDL since the physical index was already correct). All applied; makemigrations --check clean; manage.py check clean. Verified via shell (rolled back): Division>Department>Team builds; all 4 invalid tier combos rejected; hire into a Team seat → profile.org_node=Team, derived department=parent Department, line manager=reports_to holder, dept-head climb resolves up to the Department head. URL reverses OK (/v1/user/organogram/nodes/, .../positions/tree/); vs_workflow ORGANOGRAM approver path still wired.

# 0t. Active-only seat occupancy + HR profile prefill at user creation. (1) Position holder/vacancy properties now count ONLY active users: current_assignments / current_holder / current_holders / is_vacant / open_seats all filter end_date__isnull=True AND user__is_active=True. Effect: a seat reserved for a hire still in PENDING_APPROVAL/PENDING (is_active=False, e.g. the 0s creation-time assignment) is NOT counted as occupied and the hire does NOT appear as a holder until they activate their account; on activation (is_active=True) the seat flips to occupied automatically. Also means current_line_manager / Department.head won't return a not-yet-active manager. (2) UserCreateSerializer +optional HR prefill fields job_title / employee_id / employment_type / date_joined (CX-only guard; employee_id uniqueness checked; only supplied fields carried - never overwrites with blanks; collected into validated_data['profile_prefill']). UserCreationService.create_pending: for CX_STAFF, if profile_prefill present it PlatformStaffProfile.objects.update_or_create(user, defaults=prefill) BEFORE OrganogramService.assign_position - so the profile exists first and assign_position's internal cache sync lands the position immediately; InvitationService.create's later get_or_create is idempotent (finds the same profile). position handled separately via the 0s `position` field (not duplicated in prefill). Verified via shell (rolled back): prefill written to profile at create, position cache=ZZBE, PENDING hire -> is_vacant=True/holders=[]/open_seats=1, after activate -> is_vacant=False/holders=[hire]/open_seats=0. No migration (property + serializer + service only); manage.py check clean.

# 0s. Hook organogram seat selection into the CX user creation/invite flow (NO new field on User - per user constraint). UserCreateSerializer +optional `position` (accepts Position PK or code; validate() resolves to active Position, CX-only guard, sets validated_data['position_instance']). Timing decision: MATERIALISE THE SEAT AT CREATION TIME, not via a transient carried value - UserCreationService.create_pending pops position_instance and (for CX_STAFF) calls OrganogramService.assign_position(user, position) right after role assignment, writing the effective-dated primary PositionAssignment immediately (a PositionAssignment IS the domain record for user→seat, so no User field / no document_summary smuggling needed). The profile doesn't exist yet at create (CX profile is born at invite), so InvitationService.create now, after get_or_create(profile), syncs profile.position from OrganogramService.primary_position_for(user) - settling department + line manager the instant the profile exists. Rejection path: workflow_handlers.on_rejected (also covers withdrawn/cancelled which delegate to it) now closes every open PositionAssignment for the user via OrganogramService.end_assignment, so a rejected hire never keeps occupying a seat. Verified end-to-end via shell (rolled back): serializer resolves by code → create_pending writes the assignment (profile not yet created) → finalize_invitation creates profile + cache syncs (position=ZZBE, derived department=ZZENG, derived line manager = VP seat holder) → on_rejected leaves 0 open assignments. No model migration (field reverted); makemigrations --check clean, manage.py check clean.

# 0r. Organogram system (vs_user) + additive ORGANOGRAM approver strategy (vs_workflow). User decisions (AskUserQuestion): model = POSITION-BASED (seats, not people); departments = REAL HIERARCHICAL model replacing the free-text department field; history = FULL effective-dated history; matrix/dotted-line reporting = INCLUDE; scope = CX-only (no school/branch on org models - school org charts wait for the future `staff` app); climb modes = ALL FOUR (direct manager / N levels up / department head / specific position). CRITICAL constraint (verbatim): organogram approval routing must be a SECOND, opt-in way to build a workflow - the existing RBAC-permission approver resolution stays the untouched default; "I don't want to nullify the one in use now." (A) vs_user/models.py - Department (self-FK parent tree, code unique, head_position FK→Position, is_active; clean() guards self-parent + parent-chain cycles; .head derives the heading User; .ancestors()), Position (title, code unique, department FK[PROTECT], reports_to self-FK[SET_NULL] solid line, default_role FK→vs_rbac.PlatformRoleTemplate optional, headcount, is_active; clean() guards self-report + report-chain cycles; .current_holder/.current_holders/.is_vacant/.open_seats), PositionAssignment (user FK CX-staff, position FK[PROTECT], is_primary, is_acting, start_date, end_date NULL==current - FULL HISTORY; clean() enforces CX-only + "one current primary per user" in-Python since MariaDB W036 can't do conditional unique; .is_current), MatrixReport (position FK, reports_to FK, relationship_label, unique_together(position,reports_to); clean() blocks self-loop). PlatformStaffProfile CHANGED: removed line_manager FK; removed the free-text department field and instead cache the primary SEAT as position FK→Position (synced by OrganogramService on primary assign/close), so a single assignment settles the whole profile; department + current_line_manager are now DERIVED read-only properties off that cached position (department = position.department; line manager = position.reports_to.current_holder); .primary_assignment returns the effective-dated PositionAssignment for dates/acting. (B) vs_user/services/organogram.py - OrganogramService: assign_position (closes prior current primary, then creates new tenure, syncs profile.department), end_assignment (sets end_date, clears dept cache if primary), primary_position_for, manager_chain (climbs reports_to), build_tree (nested dict for serializer, roots = reports_to IS NULL), vacancies, and the four workflow resolvers resolve_direct_manager / resolve_n_levels_up / resolve_department_head (walks dept tree up to a head_position) / resolve_specific_position - all exclude the requester. (C) vs_user serializers - DepartmentInlineSerializer/PositionInlineSerializer; Department/Position/PositionAssignment/MatrixReport full serializers (parent_id/reports_to_id/department_id/etc write PKs, nested reads, model.clean() run in validate()); OrganogramNodeSerializer (recursive tree); PlatformStaffProfileSerializer reworked (position nested + position_id write; department + current_line_manager read-only DERIVED nested; line_manager/department_id fields removed); list serializer shows position + derived department. (D) vs_user views + urls - DepartmentViewSet, PositionViewSet (+@action tree, +@action vacancies), PositionAssignmentViewSet (create routes through OrganogramService.assign_position; +@action close), MatrixReportViewSet - all XVSModelViewSetMixin+ModelViewSet, rbac platform.organogram.view (reads) / .manage (writes); registered under v1/user/organogram/{departments,positions,assignments,matrix-reports}/. Profile viewset queryset updated (select_related department, dept filter accepts PK or code). (E) vs_workflow ADDITIVE integration - constants.py ApproverSource (RBAC_PERMISSION default / ORGANOGRAM) + OrganogramTarget (DIRECT_MANAGER/N_LEVELS_UP/DEPARTMENT_HEAD/SPECIFIC_POSITION); WorkflowStage +approver_source +organogram_target +organogram_levels +organogram_position FK('vs_user.Position'); services/approvers.py resolve_approvers branches on approver_source - ORGANOGRAM path calls new _organogram_base_users (graceful ImportError→[] like the RBAC path) relative to instance.requested_by, RBAC path UNCHANGED; same delegation/dedup expansion applies to both. services/templates.py builder reads approver_source/organogram_target/organogram_levels/organogram_position_code (resolved via _resolve_position by code) - all default so existing payloads unchanged. serializers.py stage read exposes the new fields + organogram_position_code; publish validate_stages accepts the new enums and requires organogram_target when source==ORGANOGRAM (+ position_code when target==SPECIFIC_POSITION). (F) Permissions - create_superuser.py PLATFORM_RESOURCES +organogram resource (view NORMAL / manage SENSITIVE+restricted) + SUPERUSER_PERMISSION_KEYS +platform.organogram.view/.manage; bootstrap re-run, both keys seeded + granted to xvs_super_admin. Migrations vs_user 0007 + vs_workflow 0006 made + APPLIED; manage.py check clean (only pre-existing W036 warnings); imports + URL reverse smoke-tested OK.

# 0q. Payments backlog - bulk payout (provider bulk submit) + daily settlement reconciliation (vs bank lines). User decisions (AskUserQuestion): bulk payout = PROVIDER BULK SUBMIT ONLY (loop the existing per-item create_transfer; no proprietary bank-file export); settlement recon = RECONCILE AGAINST vs_finance BANK LINES (match gateway-confirmed records to existing BankStatementLine rows; no new import format); open-banking (Mono/Okra) = SKIP for now. (A) Bulk payout - constants.py: PayoutBatchStatus TextChoices (DRAFT/PROCESSING/COMPLETED/PARTIALLY_COMPLETED/FAILED) + PAYOUT_BATCH_TERMINAL frozenset; PaymentAuditAction +PAYOUT_BATCH_CREATED/_SUBMITTED. models.py: PayoutBatch (entity, provider, reference unique, title, narration, status, total_amount/item_count denormalised, currency, source_account, metadata, submitted_at, created_by; is_terminal) + PayoutInstruction.batch FK (PROTECT, related_name="instructions", null/blank). services.py: refactored initiate_payout's provider-call block into shared _dispatch_transfer(payout, client=None, metadata, actor_user) (PENDING→PROCESSING on success / →FAILED+re-raise on ProviderError); create_payout_batch(entity, items, provider, source_account, title, narration, currency, actor_user) builds DRAFT batch + child PENDING instructions in one atomic txn (no submit), footing total_amount/item_count; submit_payout_batch(batch, actor_user) loops batch.instructions PENDING calling _dispatch_transfer (per-item FinanceError caught so one bad beneficiary doesn't abort the run), stamps submitted_at, recomputes status via _recompute_batch_status (DRAFT if all pending, COMPLETED if all PAID, FAILED if all failed, PARTIALLY_COMPLETED if mixed-settled, else PROCESSING); confirm_payout now calls _refresh_batch(payout) (select_for_update the batch) after every child transition so the batch aggregate tracks confirmations. Idempotent: re-submitting only retries non-terminal PENDING items. (B) Settlement reconciliation - NEW vs_payments/reconciliation.py (read-only, never writes): settlement_reconciliation(entity, start_date=None, end_date=None, provider=None) signs each gateway-confirmed record like a bank line (CollectionIntent SUCCEEDED → +amount inflow; PayoutInstruction PAID → −amount outflow), bounds by confirmed_at date + bank txn_date, then two-pass matches against vs_finance.BankStatementLine (bank_account__entity=entity): pass 1 reference (our reference OR provider_reference == line.reference), pass 2 exact signed-amount fallback, each bank line consumed once. Dataclasses SettlementRow (kind/gateway_id/reference/provider/amount/confirmed_at/matched_bank_line_id/match_basis/settled + amount_naira), UnmatchedBankLine, SettlementReconciliation (settled/unsettled counts+totals, unmatched_bank_total, is_reconciled = no unsettled gateway txns AND no unexplained bank lines). Serializers PayoutBatchSerializer (nested instructions) + PayoutBatchSummarySerializer (list, no children); PayoutInstructionSerializer +batch_id. Views (rbac payments.payout.create for POST/submit, payments.payout.view for reads, payments.report.view for recon): PayoutBatchListCreateView (GET list / POST assemble, optional {"submit":true} to dispatch immediately, per-item validation), PayoutBatchDetailView (GET detail / POST submit), SettlementReconciliationView (GET ?start_date=&end_date=&provider=, JSON summary+rows+unmatched_bank_lines). urls: /v1/payments/ payout-batches/ + payout-batches/<pk>/ + reports/settlement-reconciliation/. Migration vs_payments 0002 (PayoutBatch + batch FK + PaymentAuditAction alter). 11 new tests (4 PayoutBatchTests: assemble-without-submit, submit-dispatches-all, confirm-all→COMPLETED+2 VendorPayments, partial-failure via _FlakyProvider→PROCESSING then confirm→PARTIALLY_COMPLETED; 4 SettlementReconciliationTests: reference-match collection, amount-fallback payout, unsettled+unexplained breaks recon, date-window filters both sides; 3 API tests: create+submit batch endpoint, settlement-recon endpoint, payout list). vs_payments 27 tests pass, check clean, no vs_payments drift. Open-banking (Mono/Okra) intentionally NOT built (user "skip for now") - left as the sole remaining Payments backlog line.

# 0p. Reporting backlog - Statement of Changes in Equity (SOCE) + procurement analytics + IFRS-for-SMEs statutory export packs (all read-side, no AR-cycle scope). Three sub-slices, all built on existing denormalised AccountBalance/posting aggregates (no re-summing journals). (1) SOCE - vs_finance/reports.py: RETAINED_EARNINGS_COLUMN const; EquityMovement dataclass (key/label/account_id/code/opening/profit/contributions; closing=opening+profit+contributions + naira props) + StatementOfChangesInEquity (columns + balance_sheet_equity; total_opening/_profit/_contributions/_closing sums; is_reconciled=total_closing==balance_sheet_equity); _net_income(qs)=income−expense via _statement_rows(_net_by_account); statement_of_changes_in_equity(entity, period=None) - with period: prior_qs=periods<start, window_qs=period, as_of=period.end_date; without: prior empty, window=all, as_of=today. One column per booked EQUITY account (opening from prior, contributions=in-window movement so a Dr 3200/Cr cash dividend reads as a negative contribution) + a synthetic retained-earnings column carrying unclosed P&L (opening=prior net income, profit=window net income); reconciles to balance_sheet(as_of).total_equity. View ChangesInEquityView (rbac finance.report.view) /v1/finance/reports/changes-in-equity/ ?period= &export= (ReportTable cols Component/Opening/Profit/Contributions/Closing); 3 ChangesInEquityTests. (2) Procurement analytics - vs_procurement/reports.py (read-side, no models): spend_analysis(entity, start_date, end_date) over POSTED VendorInvoices by invoice_date, grouped by_vendor + by_category (uncategorised→UNCATEGORISED), SpendRow/SpendAnalysis (net/tax/gross/invoice_count, sorted gross desc); vendor_performance(entity, …) - POs (excl CANCELLED/REVERSED) by order_date, POSTED GRNs by received_date on-time if received≤po.expected_date (only rated when PO has expected_date), POSTED invoices, avg pay days via VendorPaymentAllocation (payment.payment_date−invoice.invoice_date), VendorPerformanceRow on_time_rate; procurement_cycle_time(entity, …) anchored on POSTED payment in window, walks alloc→invoice→PO→requisition, first-receipt cache per PO, stages req_to_po/po_to_receipt/receipt_to_invoice/invoice_to_payment + end_to_end (req.request_date→payment.payment_date), each bill measured once. Views SpendAnalysisView/VendorPerformanceView/ProcurementCycleTimeView (rbac procurement.report.view) at /v1/procurement/reports/{spend-analysis,vendor-performance,cycle-time}/; 5 ProcurementAnalyticsTests. (3) Statutory export packs - user decisions (AskUserQuestion): scope=FULL STATEMENT SET (SOFP+P&L IFRS-mapped, cash-flow + SOCE + trial-balance appendix ride along); output=JSON + CSV/Excel (reuse _maybe_export); mapping=NEW CONFIG LAYER (explicit IFRS line on each account). constants.py: IFRSLine TextChoices (PPE/INTANGIBLES/INVESTMENTS/INVENTORIES/TRADE_RECEIVABLES/CURRENT_TAX_ASSET/CASH/OTHER_CURRENT_ASSETS/SHARE_CAPITAL/RETAINED_EARNINGS/OTHER_RESERVES/LONG_TERM_BORROWINGS/TRADE_PAYABLES/CURRENT_TAX_PAYABLE/EMPLOYEE_PAYABLES/SHORT_TERM_BORROWINGS + income lines REVENUE/COST_OF_SALES/OTHER_INCOME/DISTRIBUTION_COSTS/ADMIN_EXPENSES/OTHER_EXPENSES/FINANCE_COSTS/TAX_EXPENSE) + DEFAULT_IFRS_LINE_BY_TYPE fallback. models.py: Account.ifrs_line CharField (choices, blank); migration vs_finance 0019. seed.py: DEFAULT_IFRS_LINE_BY_CODE maps the default chart (1100/1110→CASH, 1200→TRADE_RECEIVABLES, 1300→CURRENT_TAX_ASSET, 1400→INVENTORIES, 1500/1900→PPE, 2100/2150/2400→TRADE_PAYABLES, 2200/2300→CURRENT_TAX_PAYABLE, 2310/2320/2330→EMPLOYEE_PAYABLES, 3100→SHARE_CAPITAL, 3200→RETAINED_EARNINGS, 4100/4900/4910→REVENUE, 5100/5150→COST_OF_SALES, 5200/5300/5400→ADMIN_EXPENSES, 5500→FINANCE_COSTS); set on create + idempotent backfill of pre-existing unmapped accounts (never overrides an operator-set line). reports.py: statutory_pack(entity, as_of=None, period=None) REGROUPS the rows balance_sheet + income_statement already produce (never re-derives), so section totals foot to those statements by construction - IFRSLineGroup/IFRSSection/StatutoryPack dataclasses; SOFP sections non_current_assets/current_assets/equity/non_current_liabilities/current_liabilities; unclosed P&L folded into the RETAINED_EARNINGS equity line (mirrors balance_sheet) so is_balanced==assets==equity+liabilities; income lines regroup pnl income+expense rows; companions cash_flow_statement/statement_of_changes_in_equity/trial_balance bundled; _resolve_ifrs_line=account.ifrs_line or type default. View StatutoryPackView (rbac finance.report.view) /v1/finance/reports/statutory-pack/ ?as_of= &period= &export= (flat SOFP+P&L face table for CSV/Excel; JSON returns full bundle incl companion reconciliation flags); 4 StatutoryPackTests (SOFP regroups onto lines + folds RE + balances, income maps to IFRS lines, companions ride along + reconcile, unmapped account falls back to OTHER_CURRENT_ASSETS) + statutory-pack added to API export + endpoint-match tests. vs_finance 121 tests (only pre-existing reportlab-missing PDF-export 500 fails, unrelated) + vs_procurement 46 green, check clean, no new drift (only migration 0019).

# 0o. Banking/close backlog - tax remittance / filing workflow (FIRS VAT/WHT, PAYE→State IRS, Pension→PFA). User decisions (AskUserQuestion): amount basis = DERIVE FROM GL BALANCE (each filing reads the statutory liability control account's net period movement, never a re-keyed figure); tax-type model = CONFIGURABLE REGISTRY (a TaxObligation table seeded with the four Nigerian defaults, not hard-coded enums) so new levies are data not code; lifecycle = DRAFT → FILED → PAID. Design = perpetual-ledger DRAIN of the statutory liability control accounts that source transactions already park (2200 Output VAT from sales, 1300 Input VAT recoverable from purchases, 2300 WHT from vendor payments, 2310 PAYE + 2320 Pension from payroll). Key elegance: VAT input-VAT NETTING happens at FILE time (post Dr 2200/Cr 1300 for the recoverable), leaving 2200 holding exactly amount_due, so partial remittance is uniform (Dr liability/Cr bank) for VAT and non-VAT alike; penalty/interest = an adjustment journal (Dr expense/Cr liability) also at file time. constants.py: DocType.TAX_FILING="TXF"; 4 audit actions (TAX_FILING_PREPARED/_FILED/_PAID/_REJECTED); enums TaxObligationType (VAT/WHT/PAYE/PENSION/OTHER), TaxFilingFrequency (MONTHLY/QUARTERLY/ANNUAL), TaxFilingStatus (DRAFT/FILED/PAID/CANCELLED); codes OUTPUT_VAT_CODE="2200", INPUT_VAT_CODE="1300", WHT_PAYABLE_CODE="2300". exceptions.py: TaxFilingError (409). models.py: TaxObligation (entity, code unique-per-entity, name, obligation_type, liability_account[PROTECT], recoverable_account[PROTECT null - VAT only], authority_name, frequency, filing_day, is_active) + TaxFiling(FinanceDocument; obligation, period_start/end, due_date, filing_status, gross_liability/recoverable_amount/adjustment_amount/amount_due/amount_paid[MoneyField], payment_status[InvoicePaymentStatus], adjustment_account, currency, filing_reference, filed_at, filing_journal; @property balance_due; recompute_due()=gross−recoverable+adjustment; refresh_payment_status() mirrors ExpenseClaim). tax_filing.py services (each wrapped with record_rejection on FinanceError): _account_movement (POSTED JournalLine debit/credit sums for an account, optionally date-bounded), prepare_filing (DRAFT create-or-update for an (obligation, period); gross=max(credit−debit,0) of liability acct, recoverable=min(max(rdebit−rcredit,0),gross) of recoverable acct - excess input VAT carries forward in 1300), file_filing (DRAFT→FILED; rejects amount_due≤0; posts the netting + penalty journal, source=CLOSING; stamps filing_journal/filed_at), pay_filing (FILED→PAID; pay=min(amount,balance_due) or full; Dr liability/Cr bank, source=BANK; amount_paid+=pay; refresh_payment_status; sets PAID when cleared), outstanding_obligations (per active obligation: all-time payable=credit−debit, recoverable=debit−credit, net_outstanding=max(payable−max(recoverable,0),0)). seed.py: DEFAULT_TAX_OBLIGATIONS (VAT→2200/recov 1300/FIRS, WHT→2300/FIRS, PAYE→2310/State IRS day 10, PENSION→2320/PFA day 7) + seed_tax_obligations(entity) idempotent on (entity,code), called at the end of seed_chart_of_accounts (skips any obligation whose control account is absent). Serializers (TaxObligation w/ account codes; TaxFiling w/ obligation_code/_type/authority + balance_due + amount_due_naira) + REST in views_ops.py: /v1/finance/ tax-obligations [GET/POST, <pk> GET/PATCH, outstanding GET], tax-filings [GET/POST→prepare, <pk> GET, <pk>/file, <pk>/pay] (rbac finance.tax.view/.manage/.file/.pay). Migration vs_finance 0018 (TaxObligation/TaxFiling + DocType/audit-action alters). 9 new TaxFilingTests (prepare nets input vs output, prepare idempotent same period, file nets input VAT then pay clears liability, WHT no-recoverable pays full, partial remittance, file-with-penalty books expense + raises due, pay-before-file rejected+audited as FAILED, outstanding reports net, seed creates the four Nigerian obligations + idempotent). vs_finance suite green (only pre-existing reportlab-missing PDF-export 500 fails, unrelated), check clean, no new drift.

# 0n. Banking/close backlog - petty cash management (perpetual imprest). User decisions (AskUserQuestion): PERPETUAL accounting (each voucher posts to the GL the instant it happens, so the fund's current_balance mirror always equals its gl_account GL balance) over classic imprest-at-replenishment; MULTIPLE named funds per entity (one tin per custodian/desk); overdraw GUARD on the on-hand cash. constants.py: DocType.PETTY_CASH_VOUCHER="PCV"; 4 audit actions (PETTY_CASH_ESTABLISHED / _VOUCHER_POSTED / _VOUCHER_REJECTED / _REPLENISHED); PETTY_CASH_CODE="1110". seed.py: 1110 Petty Cash (asset, child of 1100 Cash/Bank) added to DEFAULT_CHART + parent wiring. exceptions.py: PettyCashError (409) + PettyCashOverdrawError (carries fund_name/requested/on_hand). models.py: PettyCashFund (entity, name unique-per-entity, gl_account[OneToOne PROTECT], custodian/custodian_name, float_amount, current_balance mirror, branch/currency, is_active; @property shortfall=max(float−current,0)) + PettyCashVoucher(FinanceDocument; fund, voucher_date, payee, spent_by, subtotal/tax_total/total, journal; recompute_totals aggregates lines) + PettyCashVoucherLine (expense_account[PROTECT], quantity, unit_price, tax_code, net_amount/tax_amount, cost_center, line_no). petty_cash.py services (each wrapped with record_rejection on FinanceError): establish_fund (Dr 1110/Cr bank, raises current_balance - opening float or permanent top-up), price_voucher (per-line net via compute_line_net + tax via compute_tax bps, roll up), post_voucher (DRAFT→POSTED: select_for_update the fund row so concurrent vouchers can't both pass the guard; Dr expense(s) aggregated by account + Dr input VAT to tax_code.paid_account, Cr 1110 for gross total; PettyCashOverdrawError if total>current_balance; lowers current_balance), replenish_fund (Dr 1110/Cr bank for the shortfall by default, or a given amount; restores to float + stamps last_replenished_at), fund_status (read-only per-fund position with needs_replenish flag at ≤threshold_bps of float, default 25%). Serializers (PettyCashFund w/ gl code + custodian_label + *_naira + shortfall; PettyCashVoucher w/ nested lines + total_naira) + REST in views_ops.py: /v1/finance/ petty-cash-funds [GET/POST, <pk> GET/PATCH, <pk>/establish, <pk>/replenish], petty-cash-status [GET], petty-cash-vouchers [GET/POST, <pk> GET, <pk>/post] (rbac finance.pettycash.view/.manage/.create/.post/.replenish). Migration vs_finance 0017 (PettyCashFund/Voucher/VoucherLine + DocType/audit-action alters). 7 new PettyCashTests (establish moves cash bank→tin, non-positive establish rejected, voucher posts Dr expense+input VAT/Cr petty cash + lowers balance, overdraw blocked+audited as REJECTED, replenish restores float by default + stamps date, replenish-with-nothing rejected, fund_status flags low balance), 105 vs_finance tests pass, check clean, no new drift.

# 0m. Procurement backlog - inventory / stock ledger (perpetual, weighted-average cost). User decisions (AskUserQuestion): NEW StockItem model separate from CatalogItem (CatalogItem = buying master-data defaults incl. services; StockItem = inventory state for the physically-held subset) with an OPTIONAL catalog_item link; weighted-average valuation; perpetual GRN→Inventory GL coupling; single implicit location; include a reorder report. Valuation is held WITHOUT floats: StockItem carries integer on_hand_qty (Decimal) + total stock_value (kobo); moving-average unit_cost is DERIVED (stock_value/on_hand_qty). Issue value = stock_value×qty/on_hand (proportion of carried value) so repeated issues never drift and the final issue empties stock_value exactly as it clears on_hand_qty. constants.py: StockMovementType (RECEIPT/ISSUE/ADJUSTMENT), INVENTORY_ASSET_CODE="1400", INVENTORY_ADJUSTMENT_CODE="5150". seed.py: 1400 Inventory (asset) + 5150 Inventory Adjustments (expense) added to DEFAULT_CHART + parent wiring. models.py: StockItem (entity, code unique-per-entity, inventory_account[PROTECT], default_expense_account, reorder_level/qty, on_hand_qty, stock_value, catalog_item[SET_NULL]) + StockMovement (immutable signed ledger row snapshotting balance_qty/balance_value, links grn + journal); GoodsReceivedNoteLine gains stock_item FK. stock.py services: receive_stock (sub-ledger only - GRN journal posts the GL), issue_stock (Dr expense, Cr inventory at moving average; InsufficientStockError guard) + adjust_stock (signed write-up Dr 1400/Cr 5150 or shrinkage Dr 5150/Cr 1400; opening write-up into empty stock needs unit_cost), each wrapped with record_rejection on FinanceError; reorder_report + stock_valuation read-only views. purchasing.py: _post_grn_atomic now routes a stock-flagged line's debit to its inventory_account (perpetual: Dr inventory not Dr expense), aggregates by debit account, and after posting calls receive_stock to raise the sub-ledger + write the RECEIPT movement. 5 STOCK_* audit actions in vs_finance; StockError/InsufficientStockError (409). Serializers (StockItem w/ derived unit_cost/needs_reorder + *_naira; StockMovement) + REST in views.py: /v1/procurement/ stock-items [GET/POST, <pk> GET/PATCH, <pk>/issue, <pk>/adjust], stock-movements [GET], reports/stock-reorder + reports/stock-valuation (rbac procurement.stock.view/.manage/.issue/.adjust, procurement.report.view). Migrations vs_procurement 0006 + vs_finance 0016. 8 new StockLedgerTests (GRN capitalises to inventory not expense, weighted-average blends two lots, issue posts Dr expense/Cr inventory at average, over-issue blocked+audited, repeated issues empty value exactly, write-up+shrinkage adjustments, opening write-up needs unit_cost, reorder report + valuation), 41 vs_procurement tests pass, check clean, no new drift.

# 0l. Procurement backlog - route PR/PO/vendor-invoice spend approvals through vs_workflow with amount thresholds (retired the direct approve endpoint). Decision: "always workflow, stages gated" (every submit creates a WorkflowInstance; NO auto-approve shortcut) + "platform-wide default" templates (school=None, branch=None) so any LedgerEntity works out of the box while branch/school templates still override via the engine's cascade. The architectural tension (vs_workflow is School/Branch + RBAC-scoped; procurement docs are LedgerEntity-scoped) is bridged by an approval_state overlay field independent of FinanceDocument.status - so VendorInvoice posting (needs status==DRAFT) is undisturbed. constants.py: ProcApprovalState (NOT_SUBMITTED/PENDING/APPROVED/REJECTED), WF_DOCTYPE_* tokens, WF_DEFAULT_TEMPLATE_CODE="standard", WF_DEFAULT_SENIOR_THRESHOLD=50_000_000 kobo (₦500k), manager/senior permission keys. models.py: each of PurchaseRequisition/PurchaseOrder/VendorInvoice gains workflow_document_type, workflow_amount_field (estimated_total / total / total) + approval_state. approvals.py: ensure_default_approval_templates() publishes one platform template per doc-type, a two-stage ladder - manager (always) + senior (inclusion_condition gte on the amount field → only high-value escalates), both PLATFORM scope / advance ANY / on_rejection TERMINAL / skip_if_no_approvers; submit_for_approval() flips approval_state→PENDING (+ requisition status DRAFT→PENDING_APPROVAL) then hands to vs_workflow; apply_approved/apply_rejected/reset_pending are the terminal callbacks (requisition→APPROVED so a PO can be raised; rejection→CANCELLED; withdraw/cancel→rollback). workflow_handlers.py: _ProcApprovalHandler + 3 @register_handler subclasses (summary shows amount via format_naira + vendor); registered from VsProcurementConfig.ready(). REST in views.py: requisitions/<pk>/submit/ now routes through workflow (RequisitionApproveView REMOVED), + purchase-orders/<pk>/submit/, vendor-invoices/<pk>/submit/, approvals/default-templates/ (rbac procurement.*.submit / procurement.approval.manage). New VENDOR_INVOICE_APPROVED audit action; ApprovalWorkflowError(409). Migrations vs_procurement 0005 + vs_finance 0015. 6 new WorkflowApprovalTests (template idempotency, auto-skip→synchronous approve, double-submit guard, manager-vote approves low-value, high-value escalates to senior stage, rejection cancels), 33 vs_procurement tests pass, check clean, no new drift.

# 0k. Procurement backlog - AP cash-requirements forecast + GR/IR aging. Two read-only reports (no model change → no migration). ap_cash_requirements(entity, *, as_of=None): the forward-looking twin of ap_aging - every POSTED, not-fully-paid bill's balance_due bucketed by days-until-due (due_date or invoice_date − as_of) into FORECAST_BUCKETS overdue/0-7/8-30/31-60/61-90/90+, per vendor, so treasury sees how much cash each window needs. grir_aging(entity, *, as_of=None): drills the single grir_balance figure into how long each received-not-invoiced position has sat - per POSTED GRN, open_value = grn.total_value − Sum(POSTED VendorInvoiceLine.net_amount where grn_line__grn=grn), aged off received_date via _bucket_for; carries control_balance = grir_balance(entity) and difference = total_open − abs(control_balance) to flag price variances / non-PO postings the GRN walk can't see (zero when clean). REST in views.py: GET /v1/procurement/reports/ap-cash-requirements/ + /reports/grir-aging/ (both ?entity=&as_of=, rbac procurement.report.view, kobo+naira money). No migration. 3 new tests, 27 vs_procurement tests pass, check clean, no drift.

# 0j. Procurement backlog - vendor contracts (renewal/expiry alerts + milestones). New master-data overlay (no GL effect). Models VendorContract (entity + vendor[PROTECT], reference unique-per-entity, title, status[ContractStatus DRAFT/ACTIVE/EXPIRED/TERMINATED/RENEWED], start/end dates, contract_value kobo, payment_terms, auto_renew, renewal_notice_days[default 30], renews[self SET_NULL], notes; renewal_window_start() = end_date − notice_days) + ContractMilestone (contract CASCADE, name, due_date, amount kobo, status[MilestoneStatus PENDING/COMPLETED/MISSED], completed_date, line_no). Services in contracts.py: activate_contract (DRAFT→ACTIVE, requires start≤end), terminate_contract (→TERMINATED, idempotent on terminal, refuses DRAFT), renew_contract (@atomic: builds an ACTIVE successor carrying vendor/terms/value, renews-FK back, optional copy of PENDING milestones; original→RENEWED), complete_milestone (→COMPLETED + completed_date, idempotent), flag_missed_milestones (PENDING past due→MISSED), mark_expired (ACTIVE past end_date→EXPIRED), expiring_contracts (renewal alerts: per-contract notice window, or within_days horizon override). 4 audit actions (VENDOR_CONTRACT_ACTIVATED/RENEWED/TERMINATED, CONTRACT_MILESTONE_COMPLETED) in vs_finance; ContractError. Serializers + REST in views.py: /v1/procurement/ contracts [GET/POST, <pk> GET/PATCH, activate/renew/terminate, milestones/<id>/complete], contracts/renewals [GET ?as_of=&within_days=] (rbac procurement.contract.view/.create/.update/.activate/.renew/.terminate). Migrations vs_procurement 0004 + vs_finance 0014. 6 new tests, 24 vs_procurement + 98 vs_finance tests pass, check clean, no drift.

# 0i. Procurement backlog - item catalog (reusable buying defaults). New master-data model CatalogItem (no GL effect; like VendorCategory) carrying per-entity-unique code + name/description/unit_of_measure and the defaults a buyer would otherwise retype on every line: preferred_vendor (SET_NULL), default_expense_account (GL the cost lands in), default_tax_code, lead_time_days (planning hint), standard_unit_price (kobo). CatalogItem.line_defaults() returns {description, expense_account, tax_code, unit_price} (description falls back to name). RequisitionListCreateView lines now accept a "catalog_item" (id|code) to seed those defaults - explicit per-line values still win. Serializer CatalogItemSerializer (+ *_code displays, *_naira). REST in views.py: GET/POST /v1/procurement/catalog-items/ (filters is_active, vendor, q search), GET/PATCH /v1/procurement/catalog-items/<pk>/ (rbac procurement.catalog_item.view/.create/.update). Migration vs_procurement 0003. 3 new tests, 18 vs_procurement tests pass, check clean, no drift.

# 0h. Procurement backlog - competitive sourcing: RFQ → vendor quotations → award → PO. New pre-PO funnel, all a sourcing overlay (no GL effect; first GL event is still the goods receipt on the resulting PO). Models RequestForQuotation (doc RFQ) + RfqLine (spec only), VendorQuotation (doc QUO) + VendorQuotationLine (priced, net/tax via compute_line_net/compute_tax). Services in sourcing.py: issue_rfq (DRAFT→ISSUED, requires lines), cancel_rfq (→CANCELLED, refuses if AWARDED, idempotent terminal), price_quotation (per-line net/tax + rollup), submit_quotation (DRAFT→SUBMITTED, needs RFQ ISSUED + lines), award_quotation (@atomic: builds a DRAFT PurchaseOrder from the winning quote's lines reusing purchasing.price_po + vendor/category expense-account fallback and carrying requisition_line via rfq_line; sets quote AWARDED + awarded_po, flips sibling SUBMITTED quotes to REJECTED, RFQ→AWARDED). DocType.QUOTATION + 4 audit actions (RFQ_ISSUED/RFQ_CANCELLED/QUOTATION_SUBMITTED/QUOTATION_AWARDED) added to vs_finance; SourcingError. Serializers + REST in views.py (/v1/procurement/ rfqs [GET/POST, <pk>, issue/cancel], quotations [GET/POST, <pk>, submit, award→201 PO]; rbac procurement.rfq.view/.create/.issue, procurement.quotation.view/.create/.submit/.award). Migrations vs_procurement 0002 + vs_finance 0013. 5 new tests, 15 vs_procurement + 98 vs_finance tests pass, check clean, no drift.

# 0g. Finance backlog - AR dunning / automated payment reminders. New communications overlay (never posts to GL). DunningPolicy → DunningStage ladder keyed by min_days_overdue (level/name/channel/message); ensure_default_policy seeds a standard 1/14/30-day, 3-stage EMAIL ladder (idempotent, one default per entity). generate_dunning(entity, *, as_of, policy, customer) scans POSTED, not-fully-paid invoices, measures days-overdue from due_date (fallback invoice_date), and raises a DunningNotice (doc DUN) at the HIGHEST qualifying stage - idempotent per (invoice, level) unique constraint so reruns escalate, never duplicate; notices on since-settled invoices flip to RESOLVED. mark_notice_sent (PENDING→SENT + sent_at) / cancel_notice (→CANCELLED). Services in dunning.py, models + migration 0012, serializers, REST in views_ar.py (/v1/finance/ dunning-policies [GET/POST, POST use_default shortcut], dunning/generate, dunning-notices [list + <pk> + send/cancel]; rbac finance.dunning.view/.manage/.generate/.send). 6 new tests, 98 vs_finance tests pass, check clean, no drift.

# 0f. Finance backlog - AR customer statements of account. New report `customer_statement(customer, *, start_date=None, end_date=None)` in reports.py builds a dated ledger of one customer's POSTED AR documents (invoices/debit notes/refunds debit; receipts/credit notes/concessions credit) with a running balance: opening_balance folds all movements before start_date (None = from inception, zero opening), entries cover [start_date, end_date] (end defaults to today), closing_balance = opening + debits − credits, plus an aging block over the customer's still-open invoice balance_due as at end_date. Write-offs clear invoices internally and aren't itemised (aging reflects live balances). REST at GET /v1/finance/reports/customer-statement/ (?entity=&customer=<code|id>&start=&end=, rbac finance.report.view, kobo+naira money, ?export=csv|xlsx|pdf) via CustomerStatementView in views_ar.py. No model change → no migration. 2 new tests, 92 vs_finance tests pass, check clean, no drift.

# 0e. Finance backlog - AR installment plans + concessions. Concessions (discount/waiver/scholarship, doc CNC) post Dr 4910 Discounts & Concessions Allowed / Cr AR and clear the invoice via amount_credited (a policy-grounded targeted credit; SCHOLARSHIP is the domain-neutral name for a school bursary). PaymentPlan (doc PPL) is a pure scheduling overlay - never posts: splits a receivable into evenly-divided dated installments (WEEKLY/FORTNIGHTLY/MONTHLY/QUARTERLY, remainder on the last), DRAFT→ACTIVE→COMPLETED/CANCELLED; refresh_plan_progress distributes the linked invoice's settled_amount across installments oldest-first and auto-completes. New 4910 contra-income account seeded. Services in installments.py, REST in views_ar.py (/v1/finance/ concessions + payment-plans w/ activate/refresh/cancel), migration 0011. 7 new tests, 90 vs_finance tests pass, check clean, no drift.

# 0d. Finance backlog - AR adjustments: credit/debit notes (CRN/DRN tokens via per-instance DOC_TYPE), customer refunds (RFD), bad-debt write-offs. New Invoice.amount_credited tracks non-cash reductions; settled_amount = amount_paid + amount_credited drives balance_due / payment status (cash logic unchanged when amount_credited=0). CREDIT note Dr revenue+output tax / Cr AR then optional oldest-first allocation against open invoices; DEBIT note reverses (Dr AR / Cr revenue+tax, cannot allocate); refund Dr AR / Cr bank; write-off Dr bad-debt 5300 / Cr AR. Services in credit_notes.py, REST in views_ar.py (/v1/finance/ credit-notes, refunds, invoices/<pk>/write-off), migration 0010. 5 new tests, 83 vs_finance tests pass, check clean, no migration drift.

# 0c. Finance Phase 6 - Payment integration (NEW app vs_payments, mounted /v1/payments/). Both directions: collections (money-in) + payouts (money-out) end-to-end, behind a provider-neutral interface with real Paystack + OPay HTTP clients (stdlib urllib) and a deterministic FakeProvider for tests. Confirmed collection → vs_finance post_payment (Dr bank/Cr AR); confirmed payout → vs_procurement post_vendor_payment (Dr AP/Cr bank/Cr WHT). Idempotent webhooks (dedupe_key + select_for_update + terminal short-circuit → retried event never double-books). Public signature-verified webhook receiver. Append-only PaymentEvent audit. 17 tests pass, check clean, no vs_payments migration drift. Credential-sourcing guide for Paystack/OPay delivered to user.

# 0b. Finance Phase 5 - Financial statements (Income Statement, Balance Sheet with unclosed net income → retained earnings, Cash Flow classified operating/investing/financing) in reports.py; DRF REST API at /v1/finance/ (entity-scoped via ?entity=<id|code>: entities/accounts/periods/journals/invoices lists, journal detail, post/reverse/close actions, six report endpoints) matching the platform envelope + RBAC; exports (CSV/Excel/PDF via ?export= on the report endpoints). 78 tests pass, check clean, no migration drift.

# 0a. Finance Phase 4 - Banking + reconciliation, expense claims, payroll (accrue/disburse), budget + variance, fixed-asset register + straight-line depreciation, period close (4-state lock, checklist, injectable extra_checks). All in vs_finance. 64 tests pass, check clean, no migration drift.

# 0. Finance Phase 3 - Procure-to-Pay (vs_procurement): PR→PO→GRN→VendorInvoice→VendorPayment with GR/IR clearing, 3-way match, WHT, AP aging/reconciliation. 10 tests pass.

# 1. A Check for if school admin and branch admin already exist in the users database and throw validation error if yes
# 2. Make module, resource and action in the permission model models of their own that form the key of a permission.
# 3. Add school/branch scoping to school role template
# 4. uid for users - unique per school (school users) and unique across Vision Staff. Starts at 10, auto-increments, uneditable.
