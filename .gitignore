# -- macos --
.DS_Store

# -- django --
*.log
*.pot
*.pyc
__pycache__/
db.sqlite3

# -- folder --
Others/
*cx/
.qodo/
frontend_2/
staticfiles/
# .claude/
apps/static/images/imports/

# -- secrets / env --
.env
.env.*
!.env.example
# Local-only onboarding doc (shared privately, references the .env handoff)
COLLEAGUE_SETUP.md

# Running note of frontend work owed by backend changes. Local-only: it is a
# working list that churns every session, not a release artifact like docs/frd/.
docs/FRONTEND_CHANGES_OWED.md

# agent worktrees (never commit)
.claude/worktrees/

# Machine-specific: launch.json holds absolute paths to one developer's
# checkout, so it works nowhere but the machine that wrote it.
.claude/launch.json
